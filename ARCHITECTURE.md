# আর্কিটেকচার — সম্পূর্ণ বর্ণনা

> **এই ফাইলের উদ্দেশ্য:** যে কেউ (মানুষ ডেভেলপার বা AI) শুধু কোডবেস পড়ে
> পুরো সিস্টেমটি বুঝতে পারবে। কোনো প্রশ্ন থাকলে আগে এখানে খুঁজুন।

---

## ১. এক নজরে

একটি **একক বিক্রেতার (single-seller) বাংলা অনলাইন বইয়ের দোকান**।

| দিক | সিদ্ধান্ত |
|---|---|
| ধরন | Single-seller bookstore (multi-vendor নয়) |
| পেমেন্ট | ক্যাশ অন ডেলিভারি + bKash/Nagad ম্যানুয়াল ভেরিফিকেশন |
| ভাষা | বাংলা (কোড কমেন্টও বাংলায়, যাতে যেকোনো বাংলাভাষী ডেভেলপার বুঝতে পারেন) |
| ক্রেতা অ্যাকাউন্ট | **নেই** — গেস্ট চেকআউট (শুধু ফোন নম্বর লাগে) |
| অ্যাডমিন | Supabase Auth (ইমেইল+পাসওয়ার্ড) + `staff` টেবিল |
| টাকা | BDT, `numeric(10,2)`, ডেটাবেসে হিসাব হয় |
| ডিপ্লয়মেন্ট | Vercel (Singapore region) |

**কেন গেস্ট চেকআউট?** বাংলাদেশে গেস্ট কাউন্টার থেকে অর্ডার স্বাভাবিক —
"অ্যাকাউন্ট খুলুন" চাইলে অনেক ক্রেতা ঝরে যায়। এতে কার্ট ডেটাবেসে রাখারও
দরকার হয় না (localStorage যথেষ্ট), ফলে সিস্টেম অনেক সরল থাকে।

---

## ২. টেক স্ট্যাক

| স্তর | প্রযুক্তি | ভার্সন | কেন |
|---|---|---|---|
| ফ্রেমওয়ার্ক | Next.js (App Router) | 16.3.6 | Vercel-এর সাথে নেটিভ, SEO বিনা-জটিলতায় |
| ভাষা | TypeScript (strict) | 5.9 | ভুল বিল্ড-টাইমেই ধরা পড়ে |
| স্টাইল | Tailwind CSS | 4.3 | কনফিগারেশন CSS-এ, `tailwind.config.js` নেই |
| ব্যাকএন্ড | Supabase (PostgreSQL) | — | ডেটাবেস + Auth + Storage একসাথে, ফ্রি টিয়ার |
| অথ ক্লায়েন্ট | `@supabase/ssr` | 0.12.7 | কুকি-ভিত্তিক SSR সেশন |
| অথ সিডিকে | `@supabase/supabase-js` | 2.117.1 | ব্রাউজার ও সার্ভার ক্লায়েন্ট |
| UI | React | 19.3.0 | — |
| হোস্টিং | Vercel | — | Next.js এর ঘরের মঞ্চ |
| টেস্ট | Vitest | 5.0 | বিশুদ্ধ ফাংশনের ৯২টি টেস্ট |
| অ্যানালিটিক্স | `@vercel/analytics` | 2.0 | কুকি-মুক্ত, ফ্রি |

**কোনো অতিরিক্ত লাইব্রেরি নেই** — state management (React Context), ফর্ম
ভ্যালিডেশন (হাতে লেখা), আইকন (ইমোজি), সব নিজে তৈরি। ফলে ডিপেন্ডেন্সি কম,
বিল্ড দ্রুত, আর যেকোনো AI সহজে পুরো কোডবেস পড়তে পারে।

---

## ৩. ডিরেক্টরি ম্যাপ

```
bookstore/
├── proxy.ts                     ★ Next.js 16 এর Proxy (আগে middleware.ts)
│                                   প্রতি রিকোয়েস্টে Supabase সেশন রিফ্রেশ করে
├── next.config.ts                  ইমেজ ডোমেইন, সিকিউরিটি হেডার
├── vercel.json                     ডিপ্লয়মেন্ট কনফিগ (Region: Singapore)
├── .env.example                    সব এনভায়রনমেন্ট ভেরিয়েবলের তালিকা
│
├── vitest.config.ts             টেস্ট কনফিগারেশন (শুধু বিশুদ্ধ ফাংশন)
├── tests/                       ৯২টি ইউনিট টেস্ট (format, validation, csv,
│                                book-import, constants, courier)
├── supabase/
│   ├── README.md                ★ ডেটাবেস সেটআপের ধাপে ধাপে গাইড
│   └── migrations/
│       ├── ..._0001_schema.sql     টেবিল, ENUM, ইনডেক্স, ট্রিগার
│       ├── ..._0002_functions.sql  ★ create_order() সহ সব RPC
│       ├── ..._0003_rls.sql        ★ Row Level Security নীতিমালা
│       ├── ..._0004_seed.sql       ক্যাটাগরি, ডেলিভারি চার্জ, ডেমো ডেটা
│       ├── ..._0005_storage.sql    ছবির bucket ও তার পলিসি
│       ├── ..._0006_rate_limit.sql ★ অর্ডার স্প্যাম ঠেকানো
│       └── ..._0007_courier.sql    ★ কুরিয়ার কলাম ও স্টেটাস সিঙ্ক
│
└── src/
    ├── app/                        Next.js App Router (রুট = ফোল্ডার)
    │   ├── layout.tsx              রুট লেআউট — হেডার, ফুটার, দুই প্রোভাইডার
    │   ├── page.tsx                হোমপেজ (হিরো, ক্যাটাগরি, ফিচার্ড বই)
    │   ├── globals.css             Tailwind import + বাংলা টাইপোগ্রাফি
    │   ├── not-found.tsx           ৪০৪ পেজ
    │   ├── robots.ts / sitemap.ts  SEO
    │   ├── books/
    │   │   ├── page.tsx            বইয়ের তালিকা — সার্চ, ফিল্টার, সর্ট, পেজিং
    │   │   └── [slug]/page.tsx     একক বইয়ের পেজ + JSON-LD
    │   ├── cart/page.tsx           কার্ট (ক্লায়েন্ট-সাইড)
    │   ├── checkout/page.tsx       চেকআউট (সার্ভার) + CheckoutForm
    │   ├── wishlist/page.tsx       ইচ্ছেতালিকা (ক্লায়েন্ট-সাইড)
    │   ├── order/[orderNumber]/    অর্ডার কনফার্মেশন (কুকি দিয়ে সুরক্ষিত)
    │   ├── track/page.tsx          অর্ডার ট্র্যাকিং
    │   ├── login/page.tsx          অ্যাডমিন লগইন
    │   ├── api/cron/
    │   │   └── courier-sync/       ★ Cron — কুরিয়ার স্টেটাস সিঙ্ক (৪০১ দিয়ে সুরক্ষিত)
    │   └── admin/                ★ অ্যাডমিন প্যানেল
    │       ├── layout.tsx          ★ staff যাচাই (দ্বিতীয় স্তরের সুরক্ষা)
    │       ├── page.tsx            ড্যাশবোর্ড — স্ট্যাট, কম স্টক সতর্কতা
    │       ├── orders/             তালিকা (CSV এক্সপোর্ট সহ), বিস্তারিত,
    │       │                       স্টেটাস/পেমেন্ট/কুরিয়ার
    │       ├── books/              তালিকা, নতুন, সম্পাদনা, ★ CSV ইমপোর্ট
    │       ├── reviews/            রিভিউ মডারেশন
    │       └── settings/           site_settings এডিটর
    │
    ├── components/
    │   ├── ui.tsx                  Button, Card, Input, Badge, Alert, Field
    │   ├── site-header.tsx         হেডার + সার্চ + কার্ট/উইশলিস্ট ব্যাজ (client)
    │   ├── site-footer.tsx         ফুটার
    │   ├── book-card.tsx           বইয়ের কার্ড (সার্ভার কম্পোনেন্ট)
    │   ├── add-to-cart-button.tsx  "কার্টে যোগ করুন" (client)
    │   ├── wishlist-button.tsx     ♡ ছোট বাটন (client)
    │   ├── checkout-form.tsx       ★ চেকআউট ফর্মের সব লজিক (client)
    │   ├── print-button.tsx        ইনভয়েস প্রিন্ট
    │   ├── logout-button.tsx
    │   └── admin/                  অ্যাডমিনের ফর্মগুলো (সব client)
    │       ├── order-forms.tsx     স্টেটাস, পেমেন্ট, নোট
    │       ├── courier-card.tsx    ★ কুরিয়ার বুকিং ও স্টেটাস সিঙ্ক
    │       ├── orders-export.tsx   ★ কুরিয়ারের পোর্টালের জন্য CSV
    │       ├── book-form.tsx       ★ বইয়ের সম্পূর্ণ ফর্ম (নতুন+সম্পাদনা)
    │       ├── image-uploader.tsx  ★ ড্র্যাগ-অ্যান্ড-ড্রপ (ছবি ছোট করে দেয়)
    │       ├── csv-import.tsx      ★ CSV প্রিভিউ ও ইমপোর্ট
    │       └── misc-forms.tsx      স্টক ±, টগল, রিভিউ, সেটিংস
    │
    ├── lib/
    │   ├── supabase/
    │   │   ├── env.ts              ★ env পড়া ও যাচাই (ভুল নাম = স্পষ্ট এরর)
    │   │   ├── public.ts           ★ কুকি-বিহীন ক্লায়েন্ট (ক্যাশযোগ্য পড়া)
    │   │   ├── server.ts           ★ কুকি-সহ ক্লায়েন্ট (RLS + সেশন)
    │   │   ├── client.ts           ব্রাউজার ক্লায়েন্ট (Auth + Storage আপলোড)
    │   │   ├── admin.ts            ⚠️ SECRET KEY — RLS বাইপাস (শুধু Cron ও ব্যাচ কাজ)
    │   │   └── proxy.ts            সেশন রিফ্রেশের আসল কোড
    │   ├── actions/
    │   │   ├── guard.ts            ★ requireStaff() — প্রতিটি অ্যাডমিন অ্যাকশনের প্রথম লাইন
    │   │   ├── orders.ts           ★ placeOrder, checkCoupon, trackOrder
    │   │   ├── admin.ts            ★ সব অ্যাডমিন মিউটেশন
    │   │   ├── courier.ts          ★ কুরিয়ার বুকিং ও স্টেটাস সিঙ্ক
    │   │   ├── book-import.ts      ★ CSV বাল্ক ইমপোর্ট
    │   │   └── wishlist.ts         উইশলিস্টের বইগুলোর তথ্য আনা
    │   ├── courier/
    │   │   ├── types.ts            ★ CourierProvider ইন্টারফেস + স্টেটাস ম্যাপ
    │   │   ├── steadfast.ts        SteadFast ইমপ্লিমেন্টেশন
    │   │   └── index.ts            প্রোভাইডার রেজিস্ট্রি (নতুন কুরিয়ার এখানে যোগ)
    │   ├── queries.ts              ★ পাবলিক ডেটা পড়ার সব ফাংশন
    │   ├── settings.ts             site_settings → টাইপড অবজেক্ট
    │   ├── cart.tsx                কার্ট Context + localStorage
    │   ├── wishlist.tsx            উইশলিস্ট Context + localStorage
    │   ├── book-import.ts          ★ CSV সারির ভ্যালিডেশন (client+server দুই জায়গায়)
    │   ├── csv.ts                  CSV পার্সার ও জেনারেটর
    │   ├── constants.ts            বিভাগ/জেলা, লেবেল, রঙ
    │   ├── format.ts               টাকা, তারিখ, বাংলা সংখ্যা
    │   └── validation.ts           চেকআউট ফর্ম ভ্যালিডেশন
    │
    └── types/
        └── database.ts             ★ সব টেবিলের TypeScript টাইপ + RPC রিটার্ন
```

★ = নতুন ডেভেলপার/AI এর প্রথমে পড়া উচিত।

---

## ৪. ডেটা মডেল

```
                          ┌──────────────┐
                          │  categories  │
                          │  (বিভাগ)      │
                          └──────┬───────┘
                                 │ 1:N
                                 ▼
┌──────────┐  1:N   ┌────────────────────┐
│  staff   │        │       books        │◄──── 1:N ──── reviews
│ (কর্মী)   │        │  (বইয়ের ক্যাটালগ)  │              (রিভিউ)
└──────────┘        └─────────┬──────────┘
      │                       │ 1:N (snapshot)
      │  changed_by           ▼
      │            ┌────────────────────┐
      │            │    order_items     │
      │            │  (অর্ডারের লাইন)     │
      │            └─────────┬──────────┘
      │                      │ N:1
      ▼                      ▼
┌─────────────────────────────────────────┐        ┌──────────────┐
│                orders                   │        │  delivery_   │
│  (অর্ডার — কাস্টমার, ঠিকানা, হিসাব,      │        │    zones     │
│   পেমেন্ট, স্টেটাস)                       │        │(ডেলিভারি চার্জ)│
└────────────────┬────────────────────────┘        └──────────────┘
                 │ 1:N                                    ▲
                 ▼                                        │ (division দিয়ে মেলানো)
      ┌──────────────────────┐              ┌──────────────┴──────┐
      │ order_status_history │              │      coupons        │
      │   (অডিট ট্রেইল)        │              │   (ডিসকাউন্ট কোড)     │
      └──────────────────────┘              └─────────────────────┘

      ┌──────────────────┐        ┌──────────────────┐
      │  site_settings   │        │  storage.objects │
      │ (key/value কনফিগ) │        │  (book-covers)   │
      └──────────────────┘        └──────────────────┘
```

### টেবিলের ভূমিকা

| টেবিল | কী রাখে | কে পড়তে পারে |
|---|---|---|
| `books` | বইয়ের সম্পূর্ণ তথ্য, দাম, স্টক | সবাই (শুধু `is_active`) |
| `categories` | বিভাগ/উপবিভাগ | সবাই (শুধু `is_active`) |
| `delivery_zones` | বিভাগভিত্তিক ডেলিভারি চার্জ | সবাই |
| `site_settings` | দোকানের কনফিগ (bKash নম্বর ইত্যাদি) | `is_public` হলে সবাই |
| `coupons` | ডিসকাউন্ট কোড | **শুধু staff** (যাচাই হয় RPC দিয়ে) |
| `reviews` | কাস্টমার রিভিউ | সবাই (শুধু `is_approved`) |
| `orders` | অর্ডার | **শুধু staff** |
| `order_items` | অর্ডারের লাইন আইটেম | **শুধু staff** |
| `order_status_history` | কে কখন স্টেটাস বদলেছে | **শুধু staff** |
| `staff` | অ্যাডমিন/কর্মীর তালিকা | নিজের row + admin সব |

### কেন `order_items` এ snapshot রাখা হয়

`title_snapshot`, `author_snapshot`, `unit_price` — এগুলো কপি করা মান।
কারণ পরে বইয়ের দাম ৪৫০ → ৫০০ টাকা করলে **পুরনো ইনভয়েস বদলে যাওয়া উচিত নয়**।
আইনি ও হিসাবের দিক থেকে এটি গুরুত্বপূর্ণ।

### কেন `books.search_text` একটি generated column

PostgreSQL এ বাংলার জন্য টেক্সট-সার্চ কনফিগারেশন নেই। তাই সব প্রাসঙ্গিক
ফিল্ড (নাম, লেখক, প্রকাশনী, ISBN, বিবরণ) lowercase করে জোড়া লাগিয়ে
`pg_trgm` GIN ইনডেক্স দেওয়া হয়েছে — `ILIKE '%শব্দ%'` দিয়েও দ্রুত চলে।

> ⚠️ `concat_ws()` ব্যবহার করা যাবে না — ওটা `STABLE`, আর generated column এ
> `IMMUTABLE` ফাংশন লাগে। তাই `||` দিয়ে `coalesce` করে জোড়া লাগানো হয়েছে।
> এই ভুলটি সহজেই হয়, তাই এখানে লেখা থাকল।

---

## ৫. নিরাপত্তার তিন স্তর

```
রিকোয়েস্ট আসে
      │
      ▼
┌─────────────────────────────────────────────┐
│ স্তর ১ — proxy.ts                           │
│ "লগইন করা আছে?"                             │
│ /admin/* এ লগইন ছাড়া ঢুকলে → /login এ পাঠায়  │
│ ⚠️ শুধু দ্রুত নক-ব্যাক, আসল সুরক্ষা নয়        │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ স্তর ২ — src/app/admin/layout.tsx            │
│ "staff কি?" → is_staff() RPC কল করে          │
│ না হলে /login?error=not_staff                │
└─────────────────┬───────────────────────────┘
                  ▼
┌─────────────────────────────────────────────┐
│ স্তর ৩ — PostgreSQL RLS + RPC  ★ আসল রক্ষা    │
│ প্রতিটি টেবিলে নীতি; SECURITY DEFINER         │
│ ফাংশনের ভেতরেও আবার is_staff() চেক            │
│ proxy.ts ভুলে গেলেও এখানে ডেটা সুরক্ষিত       │
└─────────────────────────────────────────────┘
```

### কেন তিন স্তর?

স্তর ১ ও ২ শুধু **ভালো UX** দেয় (দ্রুত রিডাইরেক্ট, পরিষ্কার এরর)।
স্তর ৩ ছাড়া কেউ `curl` দিয়ে সরাসরি Supabase API কল করে পুরো ডেটা
পড়ে ফেলতে পারত। **নিয়ম: কখনো শুধু UI লুকিয়ে সুরক্ষা করবেন না।**

### ⚠️ সবচেয়ে গুরুত্বপূর্ণ নিরাপত্তা নিয়ম

`orders` টেবিলে anon এর **কোনো** INSERT/SELECT পারমিশন নেই।
অর্ডার তৈরি হয় শুধু `create_order()` ফাংশন দিয়ে, যা:

1. কাস্টমার পাঠায় শুধু `book_id` + `quantity`
2. ফাংশন ডেটাবেস থেকে **নিজে** দাম পড়ে
3. স্টক `FOR UPDATE` দিয়ে লক করে যাচাই করে
4. ডেলিভারি চার্জ ও কুপন সার্ভারে হিসাব করে
5. সব একটা ট্রানজেকশনে করে (কিছু ব্যর্থ হলে পুরোটা রোলব্যাক)

**ফল:** কেউ DevTools খুলে দাম ৪৫০ থেকে ১ টাকা করে দিতে পারবে না।

### ভবিষ্যতে নতুন অ্যাডমিন অ্যাকশন যোগ করলে

```ts
// src/lib/actions/admin.ts এর প্যাটার্ন অনুসরণ করুন
const guard = await requireStaff();
if (!guard.ok) return FORBIDDEN;
// ... তারপর guard.supabase দিয়ে কাজ করুন
```

---

## ৬. মূল ফ্লো

### ৬.১ ক্রেতা অর্ডার করছে

```
১. /books → BookCard → "কার্টে যোগ করুন"
      └─ useCart().add() → localStorage (কোনো সার্ভার কল নেই)

২. /cart → পরিমাণ বদলানো, সরানো (সব localStorage এ)

৩. /checkout
      ├─ সার্ভার: delivery_zones + site_settings পাঠায়
      ├─ ক্লায়েন্ট: division বাছলে ডেলিভারি চার্জ লাইভ দেখায়
      ├─ কুপন লিখলে: checkCoupon() → validate_coupon() RPC
      └─ সাবমিট: placeOrder() Server Action

৪. placeOrder()  [সার্ভার]
      └─ supabase.rpc('create_order', {...})   ★ সব হিসাব এখানে
            ├─ ভ্যালিডেশন (নাম, ফোন, ঠিকানা, TrxID)
            ├─ একই বই একাধিকবার হলে কোয়ান্টিটি যোগ
            ├─ SELECT ... FOR UPDATE → স্টক লক ও যাচাই
            ├─ দাম ডেটাবেস থেকে, সাবটোটাল হিসাব
            ├─ ডেলিভারি চার্জ (বিভাগ + free_above)
            ├─ কুপন যাচাই ও ডিসকাউন্ট
            ├─ orders + order_items INSERT
            ├─ stock_qty কমানো
            ├─ order_status_history INSERT
            └─ coupons.used_count +1
      └─ সফল হলে: httpOnly কুকি (অর্ডার নম্বর + ফোন) সেট

৫. router.push('/order/BK260924-0001')
      └─ কুকি পড়ে track_order() RPC → রসিদ দেখায়
      └─ ক্লায়েন্ট কার্ট clear()
```

**ভুল হলে কী হয়?** `create_order()` এক্সেপশন ছোড়ে না — বরং
`{ ok: false, code: "OUT_OF_STOCK", message: "..." }` রিটার্ন করে।
তাই কাস্টমার বাংলায় পরিষ্কার বার্তা দেখে, আর কোনো টেকনিক্যাল এরর ফাঁস হয় না।

| `code` | মানে |
|---|---|
| `INVALID_NAME` / `INVALID_PHONE` / `INVALID_ADDRESS` | ফর্ম ভ্যালিডেশন ব্যর্থ |
| `OUT_OF_STOCK` | স্টকে যথেষ্ট নেই |
| `BOOK_INACTIVE` | বইটি বন্ধ করে দেওয়া হয়েছে |
| `MISSING_TRX` | bKash/Nagad এ TrxID দেয়নি |
| `EMPTY_CART` / `TOO_MANY_ITEMS` | কার্ট সমস্যা |

### ৬.২ অর্ডার নম্বর কীভাবে তৈরি হয়

```
BK + YYMMDD + '-' + সিকোয়েন্স
BK 260924  -   0001     →  BK260924-0001
```

`public.order_number_seq` সিকোয়েন্স থেকে আসে, তাই একই দিনে
দুই অর্ডারের নম্বর কখনো মিলবে না (ডেটাবেসই নিশ্চিত করে)।

তারিখটা `now() at time zone 'Asia/Dhaka'` দিয়ে নেওয়া — UTC নয়,
নাহলে রাত ১২টার পরের অর্ডার ভুল দিনে যেত।

### ৬.৩ ডেলিভারি চার্জের নিয়ম

```
zone = delivery_zones WHERE division = <কাস্টমারের বিভাগ> AND is_active

যদি zone পাওয়া যায়:
    fee = zone.fee
    যদি zone.free_above থাকে এবং subtotal >= free_above  →  fee = 0
নাহলে:
    fee = site_settings.delivery_default_fee  (ডিফল্ট ১২০৳)
```

চেকআউট পেজে ক্লায়েন্ট-সাইডে একই হিসাব **প্রিভিউ** হিসেবে দেখানো হয়,
কিন্তু চূড়ান্ত চার্জ নির্ধারিত হয় `create_order()` এ। দুই জায়গায় নিয়ম
একই রাখতে হবে, নাহলে কাস্টমার ভিন্ন দাম দেখবে।

### ৬.৪ স্টক কখন কমে/বাড়ে

| ঘটনা | স্টক |
|---|---|
| অর্ডার তৈরি | `- quantity` (একই ট্রানজেকশনে) |
| অর্ডার `cancelled` | `+ quantity` (একবারই, ফাংশনই নিশ্চিত করে) |
| অর্ডার `returned` | `+ quantity` (একবারই) |
| অ্যাডমিন `admin_stock_adjust()` | `± delta` (০ এর নিচে যাবে না) |

> **কেন কখনো `stock_qty` সরাসরি UPDATE করা হয় না?**
> সরাসরি UPDATE করলে নেতিবাচক স্টক তৈরি হতে পারে এবং কে কখন বদলাল তার
> হিসাব থাকে না। সবসময় RPC ব্যবহার করুন — তাতে লক ও ভ্যালিডেশন দুটোই আছে।

---

## ৬.৫ স্প্যাম অর্ডার ঠেকানো — দুই স্তর

অর্ডার স্প্যাম (স্ক্রিপ্ট দিয়ে শত শত ভুয়া COD অর্ডার) একটি বাস্তব সমস্যা —
প্রতিটি অর্ডার স্টক কমায় আর আপনার সময় নষ্ট করে।

```
কাস্টমার "অর্ডার নিশ্চিত করুন" চাপল
        │
        ▼
┌──────────────────────────────────────────────┐
│ স্তর ১ — order_rate_limit_check()  (RPC)     │
│ অ্যাপ আগেই জিজ্ঞেস করে                       │
│ না পেলে → সুন্দর বাংলা বার্তা, RPC কল হয় না  │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│ create_order()                               │
└──────────────────┬───────────────────────────┘
                   ▼
┌──────────────────────────────────────────────┐
│ স্তর ২ — BEFORE INSERT ট্রিগার                │
│ pg_advisory_xact_lock(ফোন)  ← রেস বন্ধ করে  │
│ সীমা ছাড়ালে P0001 exception                 │
│ অ্যাপ সেটা ধরে সুন্দর বার্তা বানায়            │
└──────────────────────────────────────────────┘
```

**কেন দুই স্তর?** শুধু অ্যাপের চেক থাকলে একটি স্ক্রিপ্ট ১০টি রিকোয়েস্ট
একসাথে পাঠালে সবগুলোই "সীমার নিচে" দেখে ঢুকে যেত (রেস কন্ডিশন)।
শুধু ট্রিগার থাকলে কাস্টমার কাঁচা ডেটাবেস এরর দেখত।

| সীমা | মান |
|---|---|
| ১০ মিনিটে | ৩টি অর্ডার |
| ২৪ ঘণ্টায় | ১০টি অর্ডার |
| অ্যাডমিন | লিমিটে পড়ে না |

সীমা বদলাতে: `supabase/migrations/20260924000006_rate_limit.sql` এর
`order_rate_limit_rules()` ফাংশন — এক জায়গাতেই লেখা।

> ⚠️ **সীমাবদ্ধতা:** এখন শুধু ফোন নম্বর গোনা হয়। VPN দিয়ে নম্বর বদলে
> এখনো স্প্যাম সম্ভব। `orders.ip_hash` কলাম আছে কিন্তু ব্যবহার হয় না —
> ROADMAP §৪ দেখুন।

---

## ৬.৬ কুরিয়ার ইন্টিগ্রেশন

```
অ্যাডমিন "কুরিয়ারে বুক করুন" চাপল
        │
        ▼
bookCourierOrder()  [Server Action, requireStaff()]
        ├─ অর্ডার + আইটেম আনে
        ├─ আগেই বুক হয়ে গেছে কি? → থামে
        ├─ provider খোঁজে (CourierProvider ইন্টারফেস)
        ├─ createBooking() → SteadFast API
        │     POST /create_order  { invoice, recipient_*, cod_amount }
        └─ সফল হলে:
              admin_set_courier_info()  → consignment_id + tracking_code
              admin_set_order_status()  → status = 'shipped'

এরপর প্রতিদিন একবার (Vercel Hobby এর সীমা):  /api/cron/courier-sync
        ├─ Bearer CRON_SECRET যাচাই (না মিললে ৪০১)
        ├─ secret key ক্লায়েন্ট দিয়ে orders_needing_courier_sync() (৪০টি)
        ├─ প্রতিটির জন্য GET /status_by_trackingcode/{code}
        ├─ স্টেটাস ম্যাপ: delivered → delivered, returned → returned ...
        └─ system_sync_courier_status() দিয়ে সেভ
```

### অ্যাডাপ্টার প্যাটার্ন

`src/lib/courier/types.ts` এর `CourierProvider` ইন্টারফেস মানলেই নতুন
কুরিয়ার যোগ হয় — `index.ts` এর `PROVIDERS` অ্যারেতে বসালেই অ্যাডমিন UI
স্বয়ংক্রিয়ভাবে সেটাও দেখাবে। বাকি অ্যাপের কোড ছুঁতে হয় না।

**নিরাপত্তা:** `_sync_courier_core()` (আসল কাজ) anon ও authenticated —
দুই কারোর জন্যই বন্ধ। দুটি আলাদা wrapper:

| wrapper | কে কল করে | যাচাই |
|---|---|---|
| `admin_sync_courier_status()` | অ্যাডমিন প্যানেল | `is_staff()` |
| `system_sync_courier_status()` | Cron (service role) | কেউ নয় — কারণ anon/authenticated এর EXECUTE নেই |

**অজানা স্টেটাস কী হয়?** `unknown` হয় এবং অর্ডারের স্টেটাস **বদলানো হয় না**।
ভুল ধরে নিয়ে ভুল স্টেটাস বসানোর চেয়ে না বদলানো অনেক ভালো।
`on_hold` ও `delivery_failed` ইচ্ছাকৃতভাবে `null` — হাতে দেখতে হয়।
কনসোলে লগ পড়ে, তাই পরে ম্যাপে যোগ করা সহজ।

### কুরিয়ার না থাকলেও কী চলে

| পথ | কী লাগে |
|---|---|
| CSV এক্সপোর্ট → কুরিয়ারের পোর্টালে পেস্ট | কিছুই না |
| ট্র্যাকিং কোড হাতে লিখে স্টেটাস আপডেট | কিছুই না |
| API দিয়ে অটো বুকিং | `STEADFAST_API_KEY` + `STEADFAST_SECRET_KEY` |

---

## ৬.৭ CSV বাল্ক ইমপোর্ট

```
অ্যাডমিন CSV বাছল
    ├─ ব্রাউজারে parseCsv()  → শিরোনাম + সারি
    ├─ validateBookImportRow() প্রতি সারির জন্য (client)
    └─ প্রিভিউ টেবিল: কোন লাইনে কী সমস্যা
              │
              ▼  শুধু বৈধ সারি পাঠানো হয়
        bulkImportBooks()  [Server Action, requireStaff()]
            ├─ ★ আবার validateBookImportRow() — একই ফাংশন
            ├─ category slug → uuid ম্যাপ
            ├─ ১০০ সারির চাঙ্কে INSERT
            └─ চাঙ্ক ব্যর্থ হলে সারি-ধরে আবার → নির্দিষ্ট লাইন নম্বর রিপোর্ট
```

**সবচেয়ে গুরুত্বপূর্ণ:** ভ্যালিডেশন কোড একটি (`src/lib/book-import.ts`),
কিন্তু দুটি জায়গায় চলে। ফলে প্রিভিউতে যা সবুজ, সার্ভারেও ঠিক সেটাই সফল —
দুই জায়গায় আলাদা নিয়ম লিখলে একদিন অবশ্যই অমিল হয়ে যেত।

---

## ৭. Supabase ক্লায়েন্ট — কোনটা কখন

চারটি আলাদা ক্লায়েন্ট, প্রত্যেকের নির্দিষ্ট কাজ:

| ফাইল | কী দিয়ে চলে | কখন ব্যবহার | কুকি পড়ে? |
|---|---|---|---|
| `public.ts` | Publishable key | হোমপেজ, বইয়ের তালিকা, সেটিংস | ❌ |
| `server.ts` | Publishable key | অ্যাডমিন, `placeOrder`, ট্র্যাকিং | ✅ |
| `client.ts` | Publishable key | শুধু লগইন/লগআউট | ✅ (ব্রাউজার) |
| `admin.ts` | **Secret key** ⚠️ | শুধু ব্যাচ ছবি আপলোড | ❌ |

### `public` ও `server` আলাদা কেন?

Next.js এ `cookies()` পড়লেই পেজ **dynamic** হয়ে যায় — প্রতিবার
সার্ভার রেন্ডার হয়। পাবলিক ডেটার (বইয়ের তালিকা) জন্য ইউজার সেশন লাগেই না,
তাই `public.ts` কুকি পড়ে না → পেজ **ক্যাশযোগ্য** থাকে → দ্রুত ও কম খরচ।

`admin.ts` এ `import "server-only"` আছে, তাই ভুলে কোনো Client Component এ
ইমপোর্ট করলে **বিল্ড সময়েই** এরর দেবে। এটা ইচ্ছাকৃত সুরক্ষা।

---

## ৮. ক্যাশিং

| পেজ | ধরন | রিফ্রেশ |
|---|---|---|
| `/` | Static + ISR | ৫ মিনিট (`revalidate = 300`) |
| `/books/[slug]` | Static + ISR | ৫ মিনিট |
| `/sitemap.xml` | Static + ISR | ১ ঘণ্টা |
| `/checkout` | Static + ISR | ৫ মিনিট |
| `/cart`, `/login`, `/track` | Static | ক্লায়েন্ট-সাইড লজিক |
| `/admin/**` | Dynamic (`force-dynamic`) | সবসময় তাজা |
| `/order/[orderNumber]` | Dynamic | কুকি পড়ে |
| `/books` | Dynamic | `searchParams` ব্যবহার করে |

**সেটিংস বদলালে সাথে সাথে দেখা যাবে না?** `/admin/settings` এ সেভ করলে
`revalidatePath("/")` কল হয়, তাই হোমপেজ সাথে সাথে রিফ্রেশ হয়।
বাকি পেজ ৫ মিনিটে একবার।

---

## ৯. বাংলা-নির্দিষ্ট সিদ্ধান্ত

| বিষয় | কী করা হয়েছে | কেন |
|---|---|---|
| ফন্ট | Noto Sans Bengali (`next/font`, variable) | সেল্ফ-হোস্টেড, দ্রুত, অফলাইনেও কাজ করে |
| সংখ্যা | UI তে বাংলা (৳৪৫০), ডেটাবেসে ইংরেজি (450) | হিসাব নির্ভুল, দেখতে আধুনিক |
| টাকা ফরম্যাট | `en-IN` লোকেল (১,২৫০ — লাখ-কোটি স্টাইল) | দক্ষিণ এশিয়ার প্রচলিত রীতি |
| তারিখ | `Asia/Dhaka` টাইমজোন সবসময় | সার্ভার যেখানেই থাকুক, সময় সঠিক |
| লাইন-হাইট | `1.65` (সাধারণ ১.৫ এর বদলে) | বাংলা মাত্রা উপরে-নিচে জায়গা নেয় |
| ফোন | `01[3-9]XXXXXXXX` রেজেক্স, `+880` অটো-নরমালাইজ | সব অপারেটরের নম্বর |
| সার্চ | trigram + ILIKE (FTS নয়) | PostgreSQL এ বাংলা FTS কনফিগ নেই |

---

## ১০. সীমাবদ্ধতা ও পরবর্তী কাজ

### এখনো নেই (ইচ্ছাকৃত — ধীরে ধীরে যোগ করার জন্য)

- ❌ অটোমেটেড পেমেন্ট গেটওয়ে (SSLCommerz / bKash Merchant API)
- ❌ SMS / ইমেইল নোটিফিকেশন
- ❌ কাস্টমার অ্যাকাউন্ট ও "আমার অর্ডার" পেজ
- ❌ ইংরেজি ভাষার সংস্করণ (i18n)
- ❌ Pathao / RedX অ্যাডাপ্টার (SteadFast হয়েছে)
- ❌ ব্রাউজার E2E টেস্ট (Playwright)

**যা শেষ হয়েছে:** ছবি আপলোডার, CSV ইমপোর্ট, কুরিয়ার অটো-বুকিং +
স্টেটাস সিঙ্ক, রেট লিমিট, উইশলিস্ট, অ্যানালিটিক্স, ইউনিট টেস্ট।

### টেকনিক্যাল ঋণ

| বিষয় | বর্তমান অবস্থা | সমাধান |
|---|---|---|
| টাইপ | `src/types/database.ts` হাতে লেখা | `npm run db:types` দিয়ে অটো-জেনারেট |
| রেট লিমিট | ফোন-ভিত্তিক (VPN দিয়ে ফাঁকি সম্ভব) | `orders.ip_hash` ব্যবহার করে IP-ও গোনা |
| ডেটাবেসের টেস্ট | নেই — শুধু বিশুদ্ধ ফাংশন টেস্ট | আলাদা Supabase টেস্ট প্রজেক্টে `create_order()` এর টেস্ট |
| অর্ডার কনফার্মেশন | শুধু কুকি (২৪ ঘণ্টা) | SMS/ইমেইল যোগ করা ভালো |
| Supabase ব্যাকআপ | ম্যানুয়াল CSV | Pro plan এ গেলে অটো, নাহলে মাসিক রুটিন |
| Free plan পজ | ৭ দিন ট্রাফিক না থাকলে পজ হয় | Cron দিয়ে দৈনিক হালকা পিং |

### ৭ দিন পর Supabase পজ

Free plan এ ৭ দিন রিকোয়েস্ট না হলে প্রজেক্ট পজ হয়। ট্রাফিক থাকলে
সমস্যা নেই, কিন্তু শুরুর দিকে টেস্ট না করলে পজ হতে পারে। ভবিষ্যতে
একটি Cron (Vercel Cron atau Supabase `pg_cron`) দিয়ে প্রতিদিন হালকা
একটি পিং করা যায়।

---

## ১১. সহজ শব্দকোষ

| শব্দ | মানে |
|---|---|
| **RLS** | Row Level Security — ডেটাবেস নিজেই ঠিক করে কে কোন row দেখতে পারবে |
| **RPC** | Remote Procedure Call — ডেটাবেসের ফাংশন সরাসরি কল করা (`supabase.rpc(...)`) |
| **SECURITY DEFINER** | ফাংশনটি যে কল করছে তার নয়, বরং ফাংশন বানানো ব্যক্তির ক্ষমতায় চলে |
| **Server Action** | Next.js এ সার্ভারে চালিত ফাংশন (`"use server"`) — API রুট লিখতে হয় না |
| **Proxy** | Next.js 16 এ রেনেম করা `middleware` — রিকোয়েস্ট রেন্ডারের আগে চলে |
| **ISR** | Incremental Static Regeneration — স্ট্যাটিক পেজ নির্দিষ্ট সময় পর রিফ্রেশ |
| **Publishable key** | Supabase এর পাবলিক কী (পুরনো নাম `anon`) — ব্রাউজারে যেতে পারে |
| **Secret key** | Supabase এর গোপন কী (পুরনো নাম `service_role`) — RLS বাইপাস করে |
| **Snapshot** | পুরনো ডেটার কপি, যাতে পরে মূল ডেটা বদলালেও ইতিহাস অটুট থাকে |
