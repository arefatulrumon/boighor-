# সিদ্ধান্তের রেকর্ড (ADR)

> প্রতিটি গুরুত্বপূর্ণ সিদ্ধান্ত কেন নেওয়া হলো। ভবিষ্যতে কেউ "এটা বদলানো উচিত"
> ভাবলে আগে পড়ুন — হয়তো সেই কারণটা এখনো আছে।

---

## ADR-001 — একক বিক্রেতার দোকান, মাল্টি-ভেন্ডর নয়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** `books` টেবিলে সরাসরি দাম ও স্টক থাকবে, আলাদা `sellers` টেবিল নেই।

**কারণ:** শুরুতে একজনের দোকান। মাল্টি-ভেন্ডর যোগ করলে প্রতিটি বইয়ের সাথে
sellers যোগ, কমিশন হিসাব, পেআউট সিস্টেম — এসব দরকার হতো। এখনকার সরলতা
দেরিতে ফিচার যোগ করার সুযোগ দেয়।

**ভবিষ্যতে বদলাতে হলে:** `books` এ `seller_id` কলাম যোগ করে নতুন migration,
তারপর RLS পলিসিতে "নিজের বই" শর্ত। বিদ্যমান কোড ভাঙবে না।

---

## ADR-002 — কার্ট localStorage এ, ডেটাবেসে নয়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** কার্ট ব্রাউজারে থাকে। `carts` টেবিল নেই।

**কারণ:**
- গেস্ট চেকআউটে প্রতি ভিজিটরকে auth সেশন দিতে হতো — অপ্রয়োজনীয় ভার
- ডেটাবেসে কার্ট রাখলে ৭ দিন পর পরিত্যক্ত কার্ট পরিষ্কারের কাজও লাগে
- localStorage এ কার্ট তাৎক্ষণিক, অফলাইনেও কাজ করে

**ঝুঁকি:** ডিভাইস বদলালে কার্ট হারায়। বাণিজ্যিক দিক থেকে এটা গ্রহণযোগ্য
কারণ বাংলাদেশে বেশিরভাগ ক্রেতা একবারেই অর্ডার সম্পন্ন করেন।

**নিরাপত্তা:** localStorage এর দাম বিশ্বাস করা হয় না — `create_order()`
ডেটাবেস থেকে দাম পড়ে। তাই কেউ দাম বদলে দিলেও ক্ষতি নেই।

---

## ADR-003 — অর্ডার তৈরি শুধু `create_order()` RPC দিয়ে

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** `orders` ও `order_items` টেবিলে anon এর কোনো INSERT পারমিশন নেই।

**কারণ:** ক্লায়েন্ট থেকে আসা দাম, ডেলিভারি চার্জ বা ডিসকাউন্ট বিশ্বাস করা
আর্থিকভাবে বিপজ্জনক। কেউ DevTools খুলে `total: 1` পাঠিয়ে দিলে বই পেয়ে যেত।

**কীভাবে:** ফাংশনটি `SECURITY DEFINER` + এক ট্রানজেকশনে সম্পূর্ণ কাজ করে:
স্টক `FOR UPDATE` দিয়ে লক → দাম ডেটাবেস থেকে → কুপন যাচাই →
ডেলিভারি চার্জ হিসাব → INSERT → স্টক কমানো।

**বিকল্প যা বাতিল করা হয়েছে:** Next.js API Route এ হিসাব করা। বাতিল কারণ —
লজিক দুটি জায়গায় (API + DB) থাকলে একদিন অমিল হয়ে যেত, আর ট্রানজেকশনের
নিশ্চয়তা পাওয়া কঠিন হতো।

**পরিণতি:** অর্ডারের ব্যবসার নিয়ম বদলাতে হলে SQL migration লাগবে, শুধু
TypeScript নয়। এটা ইচ্ছাকৃত — নিয়ম এক জায়গায় থাকা ভালো।

---

## ADR-004 — ভুল হলে exception নয়, `{ok: false, code}` রিটার্ন

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** প্রত্যাশিত ভুল (স্টক শেষ, ফোন ভুল) `RAISE EXCEPTION` নয় বরং
`jsonb_build_object('ok', false, 'code', 'OUT_OF_STOCK', 'message', '...')`।

**কারণ:** PostgreSQL এক্সেপশন supabase-js হয়ে জটিল স্ট্রিংয়ে রূপ নেয়,
আর সেটা সরাসরি কাস্টমারকে দেখানো যায় না। JSON রিটার্নে অ্যাপ নিজেই
বাংলায় সুন্দর বার্তা দেখাতে পারে।

**ব্যতিক্রম:** অপ্রত্যাশিত ডেটাবেস এরর (নেটওয়ার্ক, পলিসি ভায়োলেশন) তখনও
এক্সেপশন হিসেবেই আসে — কারণ সেগুলো কাস্টমারের ভুল নয়।

---

## ADR-005 — গেস্ট চেকআউট, কাস্টমার অ্যাকাউন্ট নেই

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** ক্রেতাকে অ্যাকাউন্ট খুলতে হবে না — নাম, ফোন, ঠিকানা দিলেই হয়।

**কারণ:** বাংলাদেশে ই-কমার্সে গেস্ট চেকআউট স্বাভাবিক। "অ্যাকাউন্ট খুলুন"
চাইলে ঝরে পড়ার হার বাড়ে। প্রতি অর্ডারেই ফোন নম্বর পাওয়া যায়, ফলে
সমস্যা হলে কল করা যায়।

**পরিণতি:**
- অর্ডার ট্র্যাকিংয়ের জন্য অর্ডার নম্বর + ফোন দুটোই লাগে (`track_order()` RPC)
- "আমার অর্ডার তালিকা" পেজ নেই
- ভবিষ্যতে ইচ্ছা হলে `orders.user_id` যোগ করা যাবে — বিদ্যমান অর্ডারে null থাকবে

---

## ADR-006 — অর্ডার কনফার্মেশনে কুকি, URL প্যারামিটার নয়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** `placeOrder()` দুটি `httpOnly` কুকি সেট করে (অর্ডার নম্বর + ফোন),
যেগুলো `/order/[orderNumber]` পেজ পড়ে।

**কারণ:** `/order/BK260924-0001?phone=01712345678` স্টাইলের URL ব্রাউজার
হিস্টরি, সার্ভার অ্যাক্সেস-লগ, আর `Referer` হেডারে ফোন নম্বর ছড়িয়ে দেয়।
`httpOnly` কুকি JavaScript-ও পড়তে পারে না।

**সীমা:** কুকি ২৪ ঘণ্টা থাকে; বুকমার্ক করলে পরে কাজ করবে না — তখন
`/track` পেজ ব্যবহার করতে হবে। এটি গ্রহণযোগ্য কারণ রসিদ সাধারণত সাথে সাথে
দেখা হয়।

---

## ADR-007 — দুই ধরনের Supabase ক্লায়েন্ট (public vs server)

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** পাবলিক ডেটার জন্য `lib/supabase/public.ts` (কুকি ছাড়া),
ইউজার-নির্দিষ্ট কাজে `lib/supabase/server.ts` (কুকি সহ)।

**কারণ:** Next.js এ `cookies()` কল করলেই পেজ dynamic হয়ে যায় — প্রতিবার
সার্ভার রেন্ডার, কোনো ক্যাশ নেই। বইয়ের তালিকা সবার জন্য একই, তাই সেখানে
সেশন লাগেই না। এটি আলাদা করলে হোমপেজ ও বইয়ের পেজ স্ট্যাটিক/ISR হয়ে
দ্রুত চলে এবং Supabase ব্যান্ডউইথ বাঁচে (Free plan এ ৫ GB/মাস)।

**বিপদ:** কেউ ভুলে পাবলিক ক্লায়েন্ট দিয়ে ইউজার-নির্দিষ্ট ডেটা পড়তে গেলে
সেটা ফাঁস হতে পারে। নিয়ম: `public.ts` কেবল `lib/queries.ts` এ ব্যবহার হয়,
আর সেখানে শুধু Books/Categories/Settings/Zones/Reviews পড়া হয়।

---

## ADR-008 — `staff` টেবিল, Supabase Auth-এর custom claim নয়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** কে অ্যাডমিন তা `public.staff` টেবিল রেকর্ড করে, JWT claim নয়।

**কারণ:**
- টেবিল বদলালে সাথে সাথে কার্যকর হয়; custom claim বদলাতে টোকেন রিফ্রেশ লাগে
- UI তে কর্মীর নাম/ভূমিকা দেখানো সহজ
- Supabase Dashboard এ "কেউ অ্যাডমিন বানাতে" হয় না — শুধু একটা row

**পারফরম্যান্স:** প্রতিটি পলিসিতে `is_staff()` কল হয়, যা ডেটাবেসে কোয়েরি
চালায়। এটি `STABLE SECURITY DEFINER` তাই PostgreSQL প্রতিটি স্টেটমেন্টে
একবারই চালায়। বর্তমান আকারে (<১০ জন staff) পার্থক্য নগণ্য।

---

## ADR-009 — বাংলা UI, তবে ডেটাবেসে ইংরেজি সংখ্যা

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** ডেটাবেসে `450.00` (ইংরেজি), UI তে `৳৪৫০` (বাংলা)। রূপান্তর
`toBanglaDigits()` দিয়ে শেষ মুহূর্তে।

**কারণ:** ডেটাবেসে বাংলা সংখ্যা রাখলে সব `SUM`, `ORDER BY`, `numeric` কাজ
ভেঙে পড়বে। রূপান্তর শুধু দেখানোর সময় — এটাই সঠিক স্তর।

**ব্যতিক্রম:** ফোন নম্বর, পোস্ট কোড, ট্র্যাকিং কোড কখনোই বাংলায় দেখানো হয় না।

---

## ADR-010 — Tailwind v4, `tailwind.config.js` ছাড়া

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** কনফিগারেশন `src/app/globals.css` এর `@theme` ব্লকে।

**কারণ:** Tailwind v4 এটাই এখন ডিফল্ট পদ্ধতি। আলাদা JS কনফিগ ফাইল
রাখলে দুটি সত্যের উৎস তৈরি হয়।

**ফল:** থিমের রঙ/ফন্ট বদলাতে হলে `globals.css` এ যান, `tailwind.config.js`
খুঁজতে যাবেন না — সেটি নেই।

---

## ADR-011 — হাতে লেখা ভ্যালিডেশন, Zod ছাড়া

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** `src/lib/validation.ts` এ নিজস্ব ভ্যালিডেশন।

**কারণ:** চেকআউট ফর্মে মাত্র ~১২টি ফিল্ড। একটি ডিপেন্ডেন্সি যোগ করে
বান্ডেল বাড়ানো এবং ভার্সন আপডেটের ঝুঁকি নেওয়ার প্রয়োজন নেই।
আর ডেটাবেসে `create_order()` যেকোনো অবস্থাতেই আসল যাচাই করে —
ক্লায়েন্ট ভ্যালিডেশন শুধু UX এর জন্য।

**কখন বদলাবেন:** ফর্ম ২৫+ ফিল্ডে গেলে বা nested schema দরকার হলে Zod যোগ করুন।

---

## ADR-012 — টাকা `numeric(10,2)`, float নয়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** সব আর্থিক ফিল্ড `numeric(10,2)`।

**কারণ:** `float` এ `0.1 + 0.2 = 0.30000000000000004` হয়। টাকার হিসাবে
এক পয়সার ভুলও জমে বড় সমস্যা হয়। `numeric` নির্ভুল দশমিক হিসাব দেয়।

**একই কারণে:** শতাংশ ছাড়ের হিসাবে `round(..., 2)` করা হয় এবং
ডিসকাউন্ট কখনো সাবটোটালের বেশি হতে পারে না (`least()` দিয়ে নিশ্চিত)।

---

## ADR-013 — সব business logic ডেটাবেসে, API Route এ নয়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** `src/app/api/` ফোল্ডার নেই। সব মিউটেশন Server Action দিয়ে,
আর চূড়ান্ত ভ্যালিডেশন PostgreSQL ফাংশনে।

**কারণ:**
- Server Action এ আলাদা API রুট, ফেচ, এরর হ্যান্ডলিং — সব অপ্রয়োজনীয়
- অর্ডারের নিয়ম ডেটাবেসে থাকলে ভবিষ্যতে মোবাইল অ্যাপ বা অন্য ক্লায়েন্ট
  বানালেও একই নিয়ম প্রযোজ্য হবে

**ব্যতিক্রম:** ভবিষ্যতে ওয়েবহুক (কুরিয়ার, পেমেন্ট গেটওয়ে) প্রয়োজন হলে
`app/api/webhooks/*` যোগ করুন — কারণ বাইরের সার্ভার Server Action কল করতে পারে না।

---

## ADR-014 — Next.js 16 এর Proxy কনভেনশন

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** ফাইলের নাম `proxy.ts` (রুটে), এক্সপোর্ট করা ফাংশনের নাম `proxy`।

**কারণ:** Next.js 16 এ `middleware.ts` কে ডেপ্রিকেট করে `proxy.ts` করা হয়েছে
("middleware" শব্দটি Express-এর সাথে বিভ্রান্তিকর ছিল)। পুরনো নাম দিলে
Next.js 16 চুপচাপ ফাইলটি চালাবে **না**, ফলে সেশন রিফ্রেশ বন্ধ হয়ে
ইউজার লগআউট হয়ে যাবে — কোনো এরর ছাড়াই। এটি ভয়ংকর নীরব ব্যর্থতা।

**মনে রাখুন:** `middleware.ts` নামে ফাইল বানাবেন না।

---

## ADR-016 — ছবি ব্রাউজার থেকে সরাসরি Storage এ যায়

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** ছবি আপলোড হয় ব্রাউজার থেকে সরাসরি Supabase Storage এ
(`src/components/admin/image-uploader.tsx`), কোনো সার্ভার হয়ে নয়।

**কারণ:**
- ব্রাউজারের Supabase ক্লায়েন্টে অ্যাডমিনের সেশন থাকে, আর Storage-এর RLS
  পলিসি (`book_covers_staff_insert`) যাচাই করে তিনি staff কি না।
  তাই **কোনো SERVER SECRET লাগে না** — বড় নিরাপত্তা লাভ।
- ছবি Vercel Function হয়ে গেলে ডেটা দুইবার যাতায়াত করত (ব্রাউজার → Vercel →
  Supabase)। সরাসরি গেলে অর্ধেক। Vercel এর request body সীমাও এড়ানো যায়।

**সহায়ক সিদ্ধান্ত:** আপলোডের আগেই ব্রাউজারে ছবি ছোট করা হয়
(সর্বোচ্চ ১৪০০px, WebP, quality 0.85)। Free plan এ মাত্র ১ GB স্টোরেজ —
পরীক্ষা করে দেখা গেছে প্রায় ৭০% জায়গা বাঁচে। `createImageBitmap` +
`canvas.toBlob` ব্যবহার হয়; কোনো ব্রাউজারে কাজ না করলে মূল ফাইলই যায়
(ব্যর্থ হলেও কাজ আটকায় না)।

---

## ADR-017 — রেট লিমিট দুই স্তরে

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** স্প্যাম অর্ডার ঠেকাতে দুই জায়গায় যাচাই — অ্যাপে
(`order_rate_limit_check()` RPC) আর ডেটাবেসে (BEFORE INSERT ট্রিগার)।

**কারণ:**
- **শুধু অ্যাপে থাকলে:** পরীক্ষা করার পর INSERT হওয়ার মাঝে সময় থাকে।
  একটি স্ক্রিপ্ট ১০টি রিকোয়েস্ট একসাথে পাঠালে সবগুলোই "সীমার নিচে" দেখে
  ঢুকে যাবে (রেস কন্ডিশন)।
- **শুধু ডেটাবেসে থাকলে:** কাস্টমার `RAISE EXCEPTION` এর কাঁচা এরর দেখত,
  যা বোঝা যায় না।

তাই অ্যাপ আগে জিজ্ঞেস করে **সুন্দর বাংলা বার্তার** জন্য, আর ট্রিগার শেষ
পাহারাদার হিসেবে দাঁড়ায়।

**ট্রিগারে `pg_advisory_xact_lock`:** একই ফোনের দুই রিকোয়েস্টকে একসাথে
চলতে দেয় না, ফলে গোনাটা নির্ভুল হয়। লক ট্রানজেকশন শেষে নিজেই ছেড়ে যায়।

**সীমা:** ১০ মিনিটে ৩টি, ২৪ ঘণ্টায় ১০টি। `order_rate_limit_rules()`
ফাংশনে এক জায়গায় লেখা — বদলাতে হলে একটাই জায়গা।

**⚠️ যা এখনো বাকি:** শুধু ফোন নম্বর গোনা হয়। কেউ VPN দিয়ে নম্বর বদলে
স্প্যাম করতে পারে। `orders.ip_hash` কলাম আছে কিন্তু ব্যবহার হয় না —
ROADMAP এ আছে।

---

## ADR-018 — কুরিয়ারকে প্লাগ-ইন হিসেবে বানানো, আর কী ছাড়া কী চলে

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** কুরিয়ার ইন্টিগ্রেশন একটি `CourierProvider` ইন্টারফেসের
পেছনে, আর **API কী না থাকলেও দোকান পুরোপুরি চলে**।

**কারণ:** শুরুতে API কী নেই, কুরিয়ার অ্যাকাউন্টও খোলা হয়নি। যদি ইন্টিগ্রেশন
বাধ্যতামূলক হতো, সাইট লাইভই করা যেত না। তাই তিনটি পথ একসাথে রাখা হয়েছে:

| পথ | কী লাগে | কখন ব্যবহার |
|---|---|---|
| CSV এক্সপোর্ট | কিছুই না | কুরিয়ারের পোর্টালে কপি-পেস্ট |
| ট্র্যাকিং কোড হাতে | কিছুই না | ছোট পরিমাণ অর্ডার |
| অটো বুকিং | API কী | প্রতিদিনের কাজ সহজ করতে |

**কী নিশ্চিত করা হয়েছে:** SteadFast এর `create_order`,
`status_by_trackingcode` — এন্ডপয়েন্ট, `Api-Key`/`Secret-Key` হেডার,
রিকোয়েস্ট ফিল্ড ও রেসপন্সের আকৃতি সবই যাচাই করা (অনুমান নয়)।

**চেনা-না-চেনা স্টেটাস:** কুরিয়ার নতুন কোনো স্টেটাস পাঠালে
`unknown` হয় এবং **অর্ডারের স্টেটাস বদলানো হয় না** — বরং কনসোলে লগ পড়ে।
ভুল ধরে নিয়ে ভুল স্টেটাস বসানোর চেয়ে না বদলানো অনেক ভালো।
`on_hold` ও `delivery_failed` ইচ্ছাকৃতভাবে `null` — ওগুলো হাতে দেখতে হয়।

**নিরাপত্তা:** স্টেটাস সিঙ্কের ভেতরের ফাংশন (`_sync_courier_core`)
anon ও authenticated — দুই কারোর জন্যই বন্ধ। দুটি আলাদা wrapper আছে:
`admin_sync_courier_status()` (staff, is_staff() যাচাই করে) আর
`system_sync_courier_status()` (শুধু service role, Cron এর জন্য)।

**কেন দুটি ফাংশন?** একটা ফাংশনে `if auth.uid() is null then allow`
লিখলে সেটা কাজ করত, কিন্তু ওই "সূক্ষ্ম" নিয়ম পরে কেউ ভুল বুঝে নিরাপত্তা
ফাঁক করে ফেলতে পারে। দুইটা স্পষ্ট ফাংশন অনেক নিরাপদ।

---

## ADR-019 — উইশলিস্টও localStorage এ

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** উইশলিস্ট ডিভাইসে থাকে, ডেটাবেসে নয়। শুধু `book_id` জমা হয়।

**কারণ:** ক্রেতার অ্যাকাউন্টই নেই (ADR-005), তাহলে তালিকা কোথায় রাখব?
লগইন বাধ্যতামূলক করলে ADR-005 ভেঙে যায়।

**শুধু আইডি কেন রাখা হয়:** উইশলিস্ট কার্টের চেয়ে অনেক দিন টেকে।
যদি দাম/নাম কপি করে রাখা হতো, মাস দুয়েক পরে ভুল দাম দেখানো হতো।
তাই আইডি রাখা হয়, আর পেজ খোলার সময় সার্ভার থেকে তাজা তথ্য আনা হয়।

**ভালো পার্শ্বপ্রতিক্রিয়া:** যে বই মুছে ফেলা বা বন্ধ করা হয়েছে, সেটা RLS
নিজেই ফেরত দেয় না — তাই তালিকা থেকে স্বয়ংক্রিয়ভাবে ঝরে যায়। ক্রেতাকে
"এই ২টি বই আর পাওয়া যাচ্ছে না" বলে জানানো হয়।

**সীমা:** ডিভাইস বদলালে তালিকা হারায় (কার্টের মতোই, ADR-002 দেখুন)।

---

## ADR-015 — এগ্রেস বাঁচাতে ISR, তবু স্বাচ্ছন্দ্যে ৫ মিনিট

**তারিখ:** ২০২৬-০৯-২৪ · **অবস্থা:** গৃহীত

**সিদ্ধান্ত:** পাবলিক পেজে `revalidate = 300`।

**কারণ:** Supabase Free plan এ ৫ GB/মাস এগ্রেস। প্রতি ভিজিটে সার্ভার
কোয়েরি চালালে ট্রাফিক বাড়লে সীমা শেষ হতে পারে। ৫ মিনিট ক্যাশ মানে
একই পেজ হাজারবার দেখা হলেও ডেটাবেসে একবারই যায়।

**আপস:** দাম/স্টক বদলালে ৫ মিনিট পর্যন্ত পুরনো দেখাতে পারে।
অ্যাডমিনের `revalidatePath()` কল সেটিংসের ক্ষেত্রে তাৎক্ষণিক রিফ্রেশ করে।

**পরবর্তী পদক্ষেপ:** ট্রাফিক বাড়লে সময় ১৫ মিনিটে বাড়ানো যায়, অথবা
Vercel এ `s-maxage` + `stale-while-revalidate` হেডার যোগ করা যায়।
