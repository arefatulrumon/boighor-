# বইঘর — সম্পূর্ণ প্রকল্প হ্যান্ডওভার ডকুমেন্ট

> **এই ডকুমেন্টটি কার জন্য?**
> যে কেউ (মানুষ ডেভেলপার, নতুন AI অ্যাসিস্ট্যান্ট, বা ভবিষ্যতের আপনি) এই প্রকল্পটি
> প্রথমবার দেখছেন। এটি পড়লে বুঝতে পারবেন — প্রকল্পটি কী, কেন এমনভাবে বানানো,
> কোন ফাইল কোথায় আছে এবং কী করে, আর কোন কাজ কীভাবে করতে হয়।
>
> **তারিখ:** ২০২৬-০৯-২৫ · **অবস্থা:** লাইভ ও চালু

---

# ভাগ ১ — আলোচনা, সিদ্ধান্ত ও ঘটনাক্রম

## ১.১ মূল চাহিদা

মালিক একটি **বাংলা অনলাইন বইয়ের দোকান** চেয়েছিলেন, যার শর্তগুলো ছিল:

| চাহিদা | কীভাবে পূরণ করা হয়েছে |
|---|---|
| Supabase **ফ্রি প্ল্যান** ব্যবহার করতে হবে | Free plan-এর সীমার ভেতরেই পুরো সিস্টেম ডিজাইন করা (নিচে §৩ দেখুন) |
| শুরুতেই **Vercel-এ ডিপ্লয়** করে রাখতে হবে | Git সংযুক্ত — এখন `git push` করলেই স্বয়ংক্রিয়ভাবে লাইভ হয় |
| **আস্তে আস্তে** কোড ডেভেলপ করা হবে | ৭টি স্বাধীন migration, ফিচারভিত্তিক ফাইল বিন্যাস |
| কোড নিজের স্টোরেজে রাখতে হবে | `bookstore/` ফোল্ডার — ১০৪টি ফাইল |
| **ডেভেলপার** যেন কাজ চালিয়ে যেতে পারে | `ARCHITECTURE.md`, `DEPLOY.md`, `ROADMAP.md` |
| **যেকোনো AI** যেন পুরো আর্কিটেকচার বুঝতে পারে | `AGENTS.md` — AI-এর জন্য আলাদা নির্দেশিকা, প্রতিটি ফাইলে বাংলা কমেন্ট |

**ইন্টারফেসের ভাষা:** বাংলা (কোড কমেন্টও বাংলায়, যাতে যেকোনো বাংলাভাষী ডেভেলপার বুঝতে পারেন)

## ১.২ শুরুতেই নেওয়া ৪টি সিদ্ধান্ত

কাজ শুরুর আগে যে প্রশ্নগুলো করা হয়েছিল এবং যে উত্তর দেওয়া হয়েছিল —
পরবর্তী সব ডিজাইন সিদ্ধান্ত এখান থেকেই এসেছে:

| প্রশ্ন | উত্তর | এর প্রভাব |
|---|---|---|
| দোকান কেমন? | **একক বিক্রেতা (single-seller)** | মাল্টি-ভেন্ডরের জটিলতা বাদ; `books` টেবিলে সরাসরি দাম ও স্টক |
| পেমেন্ট? | **COD + bKash/Nagad ম্যানুয়াল** | কোনো পেমেন্ট গেটওয়ে নেই; ম্যানুয়াল TrxID যাচাইয়ের সিস্টেম |
| ভাষা? | **বাংলা** | Noto Sans Bengali ফন্ট, বাংলা সংখ্যা (৳১,২৫০), ৬৪ জেলার বাংলা নাম |
| স্ট্যাক? | **Next.js + TypeScript + Tailwind** | Vercel-এর সাথে ঘরের মঞ্চ; SEO ও ডেভেলপার-বান্ধব |

## ১.৩ কী কী তৈরি হয়েছে — পর্বে পর্বে

### পর্ব ১ — মূল কাঠামো

প্রথমেই সম্পূর্ণ প্রকল্পের ভিত্তি তৈরি করা হয়:

- **৭টি ডেটাবেস migration** — টেবিল, ফাংশন, নিরাপত্তা নীতি, সিড ডেটা, স্টোরেজ, রেট লিমিট, কুরিয়ার
- **ক্রেতার পূর্ণ যাত্রা** — হোমপেজ → বইয়ের তালিকা (সার্চ/ফিল্টার/সর্ট) → একক বই → কার্ট → চেকআউট → অর্ডার কনফার্মেশন → ট্র্যাকিং
- **অ্যাডমিন প্যানেল** — ড্যাশবোর্ড, অর্ডার, বই, রিভিউ, সেটিংস
- **৬টি ডকুমেন্টেশন ফাইল**
- বিল্ড ও রানটাইম পরীক্ষা করা

### পর্ব ২ — বাকি ফিচারগুলো

মালিক "তুমি নিজে সব করে দাও" বলার পর যা যোগ হয়:

| ফিচার | কেন দরকার ছিল |
|---|---|
| **ছবি আপলোডার** | প্রতিটি বইয়ের কভারে Dashboard থেকে URL কপি করা অসহনীয় ছিল |
| **CSV বাল্ক ইমপোর্ট** | ৫০০ বই এক এক করে যোগ করা বাস্তব ছিল না |
| **কুরিয়ার ইন্টিগ্রেশন (SteadFast)** | হাতে অর্ডার কুরিয়ারে দেওয়া সময় নষ্ট করে |
| **স্বয়ংক্রিয় স্টেটাস সিঙ্ক** | "কাস্টমার পেয়েছে কি" নিজে খোঁজার দরকার না |
| **রেট লিমিট (২ স্তর)** | স্ক্রিপ্ট দিয়ে শত শত ভুয়া COD অর্ডার ঠেকাতে |
| **৯২টি ইউনিট টেস্ট** | ভবিষ্যতে কেউ কোড বদলালে ভুল ধরা পড়ে |
| **ইচ্ছেতালিকা, অ্যানালিটিক্স** | ক্রেতার অভিজ্ঞতা ও দোকানের পরিমাপ |

## ১.৪ যে ভুলগুলো ধরা পড়েছে ও ঠিক করা হয়েছে

এই প্রকল্পে চারটি আসল সমস্যা ধরা পড়েছিল এবং প্রতিটির সমাধান করা হয়েছে।
**ভবিষ্যতে একই ভুল যাতে না হয়, তা জেনে রাখা জরুরি:**

### ক. `formatTaka(null)` এর অসামঞ্জস্য

টেস্ট লেখার সময় ধরা পড়ে: `formatTaka(null)` ফাংশনটি `symbol` অপশন উপেক্ষা করে সবসময় `৳০` ফেরত দিচ্ছিল, অন্য সব ক্ষেত্রে `০ টাকা` আসত। ফলে UI-এ অসামঞ্জস্য দেখা দিত।

**সমাধান:** ব্যর্থ হওয়ার পথেও অপশন মানা হয়। **শিক্ষা:** ইউনিট টেস্ট আসল বাগ ধরতে পারে।

### খ. Vercel Hobby প্ল্যানের Cron সীমা

`vercel.json`-এ Cron দেওয়া ছিল `0 */6 * * *` (প্রতি ৬ ঘণ্টায়)। Vercel-এর অফিসিয়াল ডক অনুযায়ী **Hobby প্ল্যানে Cron দিনে একবারের বেশি চলতে পারে না — এর বেশি হলে deployment-ই ব্যর্থ হয়**।

**সমাধান:** `0 3 * * *` (রাত ৩টা UTC = সকাল ৯টা বাংলাদেশ)। **শিক্ষা:** ডিপ্লয় ফেল করার কারণ সবসময় কোডে থাকে না — প্ল্যাটফর্মের সীমাও থাকতে পারে।

### গ. Migration দ্বিতীয়বার চালালে ভেঙে যেত

SQL Editor-এ স্ক্রিপ্ট দ্বিতীয়বার চালালে `type "order_status" already exists` (42710) এররে থেমে যেত। কারণ PostgreSQL-এ `create type if not exists`, `create policy if not exists` বা `create trigger if not exists` — কিছুই নেই।

**সমাধান:** `create type`-গুলো `DO` ব্লকে মোড়া (duplicate_object ধরা হয়), `create table`/`create index`-এ `if not exists`, `create trigger`/`create policy`-এর আগে `drop ... if exists`। এখন যতবার চালানো হোক, একই ফল। **শিক্ষা:** "ডকুমেন্টেশন যা দাবি করে, কোড তা মানে কি না — যাচাই করতে হয়।"

### ঘ. Secret key ব্রাউজারে কাজ করে না

Supabase-এর নতুন secret key (`sb_secret_...`) দিয়ে ব্রাউজার/সাধারণ টুল থেকে API কল করলে **401** আসে। কারণ Supabase `User-Agent` হেডার দেখে ব্রাউজার শনাক্ত করে secret key ব্লক করে (নিরাপত্তার জন্য)।

**সমাধান:** টুলে `User-Agent` হেডার সেট করা। **শিক্ষা:** 401 মানেই কী ভুল নয়।

## ১.৫ এখনো যা বাকি

### 🔴 অবশ্যই করতে হবে

**১. Supabase-এ Site URL ঠিক করা**

Supabase → **Authentication → URL Configuration**:

- **Site URL:** `https://boighor-ochre.vercel.app`
- **Redirect URLs** (দুটোই যোগ করুন):
  ```
  https://boighor-ochre.vercel.app/**
  http://localhost:3000/**
  ```

এই কাজটা না করার ফলে ইমেইলের সব লিংক (invite/reset) `http://localhost:3000`-এ যাচ্ছিল এবং "This site can't be reached" দেখাচ্ছিল।

> ⚠️ **এটি API দিয়ে করা যায় না।** Supabase-এর Auth কনফিগ শুধু **Management API** দিয়ে বদলানো যায়, যার জন্য আলাদা `sbp_...` (Personal Access Token) দরকার। প্রজেক্টের publishable/secret key দুটি **Data API**-এর চাবি — Auth কনফিগে হাত দেয় না। তাই এটি হাতে করতে হবে।

**২. পাসওয়ার্ড বদলানো**

বর্তমান অ্যাডমিন পাসওয়ার্ডটি একটি চ্যাট কথোপকথনে দেওয়া হয়েছিল, তাই এটি সাময়িক। নতুন পাসওয়ার্ড সেট করতে Supabase → Authentication → Users → নিজের ইমেইল → **Reset password**। (ওই লিংক কাজ করবে Site URL ঠিক করার পর।)

### 🟡 করলে ভালো

- **ডেমো বই মুছে আসল বই যোগ করুন** — CSV ইমপোর্টই সবচেয়ে দ্রুত পথ
- **সাইট সেটিংস পূরণ করুন** — `/admin/settings`-এ bKash/Nagad নম্বর, ফোন, ঠিকানা
- **একটি টেস্ট অর্ডার দিন** — COD ও bKash দুটোই; এটাই `create_order()` ফাংশনের আসল পরীক্ষা
- **কুরিয়ার সংযুক্ত করুন** (ইচ্ছা হলে) — `supabase/README.md` §৭
- **GitHub repo Private করুন** — Settings → Danger Zone → Change visibility

### 🟢 পরবর্তী ফিচার

[`ROADMAP.md`](ROADMAP.md)-এ অগ্রাধিকার অনুযায়ী সাজানো আছে: SMS নোটিফিকেশন, কাস্টমার অ্যাকাউন্ট, Pathao/RedX অ্যাডাপ্টার, ব্রাউজার E2E টেস্ট, i18n।

---

# ভাগ ২ — ফাইল ম্যাপ

**প্রকল্পের মূল ফোল্ডার:** `bookstore/`
**মোট ফাইল:** ১০৪টি (`node_modules`, `.next`, `.git` বাদ দিয়ে)

প্রতিটি ফাইলের পাশে **ধরন** ও **ভূমিকা** দেওয়া আছে। ধরন অনুযায়ী:
`.tsx` = TypeScript + React (UI), `.ts` = TypeScript (লজিক), `.sql` = ডেটাবেস, `.md` = ডকুমেন্টেশন, `.json` = কনফিগ।

---

## ২.১ রুট ফাইল — প্রকল্পের গেট

| ফাইল | ধরন | ভূমিকা |
|---|---|---|
| `package.json` | JSON | নির্ভরতা ও কমান্ডের তালিকা। `npm run dev/build/test/check/db:bundle` এখানে সংজ্ঞায়িত |
| `package-lock.json` | JSON | নির্ভরতার হুবহু সংস্করণ (হাতে এডিট করবেন না) |
| `tsconfig.json` | JSON | TypeScript কনফিগ। `@/*` → `./src/*` পথের সংক্ষিপ্ত রূপ এখানে |
| `next.config.ts` | TS | Next.js কনফিগ। Supabase Storage-এর ছবি অনুমোদন (`remotePatterns`) এখানে — **নতুন ছবির ডোমেইন যোগ করলে এখানে বসাতে হবে** |
| `postcss.config.mjs` | JS | Tailwind v4-এর PostCSS প্লাগিন |
| `vitest.config.ts` | TS | টেস্ট কনফিগারেশন (শুধু বিশুদ্ধ ফাংশনের টেস্ট) |
| `vercel.json` | JSON | Vercel কনফিগ — **Singapore region** ও **Cron সময়সূচি**। ⚠️ Hobby প্ল্যানে Cron দিনে একবারের বেশি দেওয়া যাবে না, নাহলে ডিপ্লয় ফেল করবে |
| `proxy.ts` | TS | Next.js 16-এর Proxy ফাইল (আগের নাম `middleware.ts`)। প্রতিটি রিকোয়েস্টে Supabase সেশন রিফ্রেশ করে। **⚠️ Next.js 16-এ ফাইলের নাম `proxy.ts` ও ফাংশনের নাম `proxy`** |
| `.env.local` | env | **আসল গোপন চাবি এখানে** (git-ignored)। প্রতিটি নতুন ডিভাইসে `.env.example` কপি করে এটি বানাতে হবে |
| `.env.example` | env | গোপন চাবির ছাঁচ — কোন কোন ভেরিয়েবল লাগবে তার তালিকা (নকল মান সহ, কমিট করা নিরাপদ) |
| `.gitignore` | text | কী কী Git-এ যাবে না (সবচেয়ে গুরুত্বপূর্ণ: `.env*.local`, `node_modules`, `.next`) |
| `.gitattributes` | text | ফাইলের লাইন-এন্ডিং নিয়ন্ত্রণ (LF)। Windows-এ লেখা কোড Linux-এ বিল্ড হওয়ার সময় সমস্যা আটকায় |
| `next-env.d.ts` | TS | Next.js স্বয়ংক্রিয়ভাবে বানায় — **হাতে এডিট করবেন না** |
| `tsconfig.tsbuildinfo` | binary | TypeScript-এর বিল্ড ক্যাশ — গিটে যায় না |

---

## ২.২ ডকুমেন্টেশন — ৭টি ফাইল

এই ফাইলগুলোই প্রকল্প বোঝার মূল চাবি। **নতুন কেউ এলে এই ক্রমে পড়ানো উচিত:**

| ফাইল | আকার | ভূমিকা |
|---|---|---|
| `README.md` | ১৬ KB | **প্রথমে এটি পড়ুন।** প্রকল্প পরিচিতি, সব ফিচারের তালিকা, ১০ মিনিটে চালু করার গাইড, কমান্ড, দৈনিক কাজের ধারা |
| `ARCHITECTURE.md` | ৪৫ KB | **সবচেয়ে গুরুত্বপূর্ণ।** সম্পূর্ণ আর্কিটেকচার — ডেটা মডেল, নিরাপত্তার ৩ স্তর, প্রতিটি ফ্লো-ডায়াগ্রাম, ব্যবসার নিয়ম, ক্যাশিং |
| `AGENTS.md` | ২১ KB | **AI অ্যাসিস্ট্যান্টের জন্য নির্দেশিকা।** দৃঢ় নিয়ম, কাজের রেসিপি, ডিবাগিং টেবিল। ⚠️ AI-কে দিলে এটি সবার আগে দিতে হবে |
| `DEPLOY.md` | ১৩ KB | Vercel-এ লাইভ করার ধাপে ধাপে গাইড + সমস্যার সমাধান |
| `DECISIONS.md` | ২৭ KB | **১৯টি সিদ্ধান্তের রেকর্ড (ADR)** — কেন এমনভাবে বানানো হলো। কিছু বদলানোর আগে পড়ুন, হয়তো সেই কারণটা এখনো আছে |
| `ROADMAP.md` | ১২ KB | আগামী কাজের পরিকল্পনা — ধাপ ১/২/৩ ভাগে, কোনটা হয়েছে তা ✓ দিয়ে চিহ্নিত |
| `HANDOVER.md` | এই ফাইল | আলোচনার ইতিহাস + ফাইল ম্যাপ + চলমান তথ্য |

---

## ২.৩ `supabase/` — ডেটাবেসের সবকিছু

ডেটাবেসের সম্পূর্ণ সংজ্ঞা এখানে। **`migrations/`-ই সত্যের একমাত্র উৎস।**

| ফাইল | ধরন | ভূমিকা |
|---|---|---|
| `supabase/README.md` | MD | ডেটাবেস সেটআপের পূর্ণ গাইড — প্রজেক্ট তৈরি, migration চালানো, ছবির bucket, কুরিয়ার সংযোগ, ব্যাকআপ, সমস্যার সমাধান |
| `supabase/RUN_ALL_IN_SQL_EDITOR.sql` | SQL | **সাতটি migration এক ফাইলে জোড়া** — SQL Editor-এ একবার পেস্ট করলেই পুরো ডেটাবেস তৈরি। ⚠️ এটি `npm run db:bundle` দিয়ে **জেনারেট হয়** — হাতে এডিট করলে পরেরবার মুছে যাবে |

### `supabase/migrations/` — ক্রম অনুযায়ী চালাতে হবে

| ফাইল | কী তৈরি করে |
|---|---|
| `20260924000001_schema.sql` | ১১টি টেবিল, ৭টি ENUM টাইপ, ইনডেক্স, ট্রিগার |
| `20260924000002_functions.sql` | ব্যবসার নিয়মের ফাংশন — `create_order()`, `track_order()`, `validate_coupon()`, `is_staff()` |
| `20260924000003_rls.sql` | ১৮টি Row Level Security নীতি — **এটাই আসল নিরাপত্তা** |
| `20260924000004_seed.sql` | শুরুর ডেটা — ৫টি ডেমো বই, ১৫টি ক্যাটাগরি, ৮টি ডেলিভারি জোন, ১৩টি সেটিং |
| `20260924000005_storage.sql` | `book-covers` bucket ও তার নিরাপত্তা নীতি |
| `20260924000006_rate_limit.sql` | অর্ডার স্প্যাম ঠেকানোর দুই স্তর + ব্যবসার সীমা পরিবর্তনের ফাংশন |
| `20260924000007_courier.sql` | কুরিয়ারের কলাম ও স্টেটাস সিঙ্কের ফাংশন |

**⚠️ নিয়ম:** ইতিমধ্যে চালানো migration **এডিট করবেন না** — নতুন ফাইল যোগ করুন। কারণ পুরনো ফাইল বদলালে ডেটাবেস আর কোডের মধ্যে ফারাক তৈরি হয়।

---

## ২.৪ `src/app/` — পেজ (Next.js App Router)

ফোল্ডারের গঠনই URL ঠিক করে দেয়। যেমন `src/app/books/page.tsx` → `/books`।

### সাধারণ পেজ

| ফাইল | URL | ভূমিকা |
|---|---|---|
| `layout.tsx` | — | মূল খাপ — বাংলা ফন্ট, হেডার, ফুটার, কার্ট/উইশলিস্ট প্রোভাইডার, অ্যানালিটিক্স |
| `page.tsx` | `/` | হোমপেজ — হিরো, ক্যাটাগরি চিপ, নির্বাচিত বই, সদ্য এসেছে |
| `globals.css` | — | Tailwind ইমপোর্ট + বাংলা টাইপোগ্রাফি। **Tailwind v4-এ কনফিগ এখানেই `@theme`-এ** (আলাদা `tailwind.config.js` নেই) |
| `not-found.tsx` | — | ৪০৪ পেজ |
| `robots.ts` | `/robots.txt` | সার্চ ইঞ্জিনকে কী দেখাবে / কী দেখাবে না |
| `sitemap.ts` | `/sitemap.xml` | সব বই ও ক্যাটাগরির তালিকা |

### ক্রেতার পেজ

| ফাইল | URL | ধরন | ভূমিকা |
|---|---|---|---|
| `books/page.tsx` | `/books` | সার্ভার | বইয়ের তালিকা — সার্চ, ক্যাটাগরি ফিল্টার, ৫ ধরনের সর্ট, পেজিনেশন |
| `books/[slug]/page.tsx` | `/books/<slug>` | সার্ভার | একক বইয়ের পেজ + রিভিউ + JSON-LD (SEO) |
| `cart/page.tsx` | `/cart` | ক্লায়েন্ট | কার্ট (localStorage থেকে পড়ে) |
| `checkout/page.tsx` | `/checkout` | সার্ভার | চেকআউটের খাপ — ডেলিভারি জোন ও সেটিংস এনে ফর্মে দেয় |
| `order/[orderNumber]/page.tsx` | `/order/<নম্বর>` | সার্ভার | অর্ডার কনফার্মেশন রসিদ (কুকি দিয়ে সুরক্ষিত) |
| `track/page.tsx` | `/track` | ক্লায়েন্ট | অর্ডার নম্বর + ফোন দিয়ে ট্র্যাকিং |
| `wishlist/page.tsx` | `/wishlist` | ক্লায়েন্ট | ইচ্ছেতালিকা |

### প্রমাণীকরণ (Authentication)

| ফাইল | URL | ভূমিকা |
|---|---|---|
| `login/page.tsx` | `/login` | অ্যাডমিন লগইন + **"পাসওয়ার্ড ভুলে গেছেন?"** ফ্লো |
| `auth/confirm/page.tsx` | `/auth/confirm` | **ইমেইল লিংক ধরার পেজ** — invite/reset লিংকের টোকেন ধরে সেশন বানায়, তারপর নতুন পাসওয়ার্ড নেওয়ার ফর্ম দেখায়। `#access_token=` (implicit) ও `?code=` (PKCE) — দুটোই সামলায় |

### অ্যাডমিন প্যানেল — `src/app/admin/`

| ফাইল | URL | ভূমিকা |
|---|---|---|
| `layout.tsx` | — | অ্যাডমিন খাপ + **`is_staff()` যাচাই (নিরাপত্তার ২য় স্তর)**। staff না হলে `/login`-এ পাঠায় |
| `page.tsx` | `/admin` | ড্যাশবোর্ড — নতুন অর্ডার, আজকের বিক্রি, পেমেন্ট যাচাই বাকি, কম স্টকের সতর্কতা |
| `orders/page.tsx` | `/admin/orders` | অর্ডার তালিকা — ফিল্টার, সার্চ, **কুরিয়ারের CSV এক্সপোর্ট** |
| `orders/[id]/page.tsx` | `/admin/orders/<id>` | অর্ডারের বিস্তারিত — সব কাজ এক জায়গায় |
| `books/page.tsx` | `/admin/books` | বইয়ের তালিকা — ইনলাইন স্টক ±, চালু/বন্ধ টগল |
| `books/new/page.tsx` | `/admin/books/new` | নতুন বই যোগ |
| `books/[id]/page.tsx` | `/admin/books/<id>` | বই সম্পাদনা |
| `books/import/page.tsx` | `/admin/books/import` | **CSV বাল্ক ইমপোর্ট** — একসাথে ২০০০ বই |
| `reviews/page.tsx` | `/admin/reviews` | রিভিউ মডারেশন (অপেক্ষায় / অনুমোদিত) |
| `settings/page.tsx` | `/admin/settings` | সাইট সেটিংস — দোকানের নাম, bKash নম্বর, ঘোষণা বার |

### API

| ফাইল | URL | ভূমিকা |
|---|---|---|
| `api/cron/courier-sync/route.ts` | `/api/cron/courier-sync` | **Cron এন্ডপয়েন্ট** — প্রতিদিন একবার পাঠানো অর্ডারের স্টেটাস কুরিয়ার থেকে টেনে আনে। `CRON_SECRET` দিয়ে সুরক্ষিত (না মিললে 401) |

---

## ২.৫ `src/components/` — UI অংশগুলো

| ফাইল | ধরন | ভূমিকা |
|---|---|---|
| `ui.tsx` | সার্ভার | **মূল UI যন্ত্রাংশ** — Button, Input, Field, Select, Card, Badge, Alert, EmptyState, ButtonLink, `cn()`। নতুন UI বানানোর আগে এখানে দেখুন |
| `site-header.tsx` | ক্লায়েন্ট | হেডার — নেভিগেশন, সার্চ, কার্ট ও উইশলিস্ট গণনা |
| `site-footer.tsx` | সার্ভার | ফুটার — যোগাযোগ, লিংক |
| `book-card.tsx` | সার্ভার | বইয়ের কার্ড (তালিকা ও হোমপেজে ব্যবহৃত) |
| `add-to-cart-button.tsx` | ক্লায়েন্ট | "কার্টে যোগ করুন" — স্টক শেষ হলে নিষ্ক্রিয় |
| `wishlist-button.tsx` | ক্লায়েন্ট | হার্ট বাটন |
| `checkout-form.tsx` | ক্লায়েন্ট | চেকআউট ফর্ম — ভ্যালিডেশন, ডেলিভারি চার্জ হিসাব, কুপন, অর্ডার |
| `print-button.tsx` | ক্লায়েন্ট | প্রিন্ট বাটন (`window.print()`) |
| `logout-button.tsx` | ক্লায়েন্ট | লগআউট |

### `src/components/admin/` — অ্যাডমিনের ফর্মগুলো

| ফাইল | ভূমিকা |
|---|---|
| `order-forms.tsx` | অর্ডারের তিনটি ফর্ম — স্টেটাস আপডেট, পেমেন্ট যাচাই, অ্যাডমিন নোট |
| `courier-card.tsx` | কুরিয়ারে বুকিং + স্টেটাস সিঙ্ক (API কী না থাকলে নিষ্ক্রিয়) |
| `orders-export.tsx` | অর্ডারের CSV এক্সপোর্ট — কুরিয়ারের পোর্টালে পেস্ট করার জন্য |
| `book-form.tsx` | বই যোগ/সম্পাদনার পূর্ণ ফর্ম — দাম ও ছাড়ের লাইভ প্রিভিউ |
| `image-uploader.tsx` | **ড্র্যাগ-অ্যান্ড-ড্রপ ছবি আপলোড** — কভার (একটি) ও গ্যালারি (১২টি পর্যন্ত)। আপলোডের আগেই WebP-তে ছোট করে (≈৭০% জায়গা বাঁচে) |
| `csv-import.tsx` | CSV ইমপোর্ট — পার্সিং, সারি-ভিত্তিক প্রিভিউ, ভুলের তালিকা |
| `misc-forms.tsx` | ছোট নিয়ন্ত্রণ — স্টক ±, চালু/বন্ধ টগল, রিভিউ অনুমোদন, সেটিংস ফর্ম |

---

## ২.৬ `src/lib/` — ব্যবসার লজিক

**এখানেই আসল বুদ্ধিমত্তা।** বাইরের নির্ভরতা যত কম, বোঝা তত সহজ।

### Supabase ক্লায়েন্ট — ৫টি আলাদা কারণ ৫টি আলাদা কাজ

⚠️ **এই ফাইলগুলো গুলিয়ে ফেলা সবচেয়ে সহজ ভুল।** কোনটা কখন:

| ফাইল | কখন ব্যবহার হয় | নিরাপত্তা |
|---|---|---|
| `supabase/env.ts` | সবার আগে, এক জায়গা থেকে env পড়া। ভুল নাম হলে সাথে সাথে স্পষ্ট এরর | — |
| `supabase/public.ts` | **পাবলিক ডেটা পড়া** (বই, ক্যাটাগরি, সেটিংস)। কুকি পড়ে না, তাই পেজ স্ট্যাটিক হতে পারে | anon — RLS প্রযোজ্য |
| `supabase/server.ts` | **সার্ভারে, লগইন-নির্ভর কাজ** — Server Action, Server Component | ইউজারের সেশন — RLS প্রযোজ্য |
| `supabase/client.ts` | **ব্রাউজারে** (Client Component) — লগইন, ছবি আপলোড | ইউজারের সেশন — RLS প্রযোজ্য |
| `supabase/proxy.ts` | `proxy.ts` থেকে ডাকা হয় — সেশন টোকেন রিফ্রেশ | — |
| `supabase/admin.ts` | ⚠️⚠️ **SECRET KEY — RLS সম্পূর্ণ বাইপাস!** শুধু Cron ও ব্যাচ কাজে | সর্বোচ্চ ক্ষমতা |

### ডেটা ও ফরম্যাট

| ফাইল | ভূমিকা |
|---|---|
| `queries.ts` | পাবলিক ডেটা পড়ার সব ফাংশন — বইয়ের তালিকা, সার্চ, একক বই, ক্যাটাগরি |
| `settings.ts` | `site_settings` টেবিল → টাইপড অবজেক্ট। ডিফল্ট মানসহ (Supabase না পেলেও সাইট চলবে) |
| `format.ts` | টাকার ফরম্যাট (`৳১,২৫০` / `১২৫০ টাকা`), বাংলা সংখ্যা (`১২৩৪`), তারিখ, স্টকের অবস্থা |
| `constants.ts` | **সব স্থির তথ্য এক জায়গায়** — ৮ বিভাগ, ৬৪ জেলা, স্টেটাসের বাংলা নাম ও রঙ, কুরিয়ারের তালিকা। ✅ টেস্ট করা |
| `validation.ts` | ফর্মের ভ্যালিডেশন — বাংলাদেশি ফোন নম্বর, ইমেইল, চেকআউটের সব নিয়ম। ✅ টেস্ট করা |
| `csv.ts` | নিজে লেখা CSV পার্সার ও জেনারেটর — কোটেশনের ভেতরের কমা, নতুন লাইন, BOM — সব সামলায়। ✅ টেস্ট করা |
| `book-import.ts` | CSV → বইয়ের ভ্যালিডেশন। **ক্লায়েন্ট ও সার্ভার দুই জায়গাতেই ব্যবহৃত** — তাই প্রিভিউতে যা সবুজ, সার্ভারেও তাই সফল। ✅ টেস্ট করা |
| `cart.tsx` | কার্ট (React Context + localStorage)। দাম শুধু দেখানোর জন্য — আসল হিসাব ডেটাবেসে |
| `wishlist.tsx` | ইচ্ছেতালিকা (শুধু book_id সংরক্ষণ করে) |

### Server Actions — ডেটা লেখার সব জায়গা

| ফাইল | ভূমিকা |
|---|---|
| `actions/guard.ts` | **`requireStaff()`** — প্রতিটি অ্যাডমিন অ্যাকশনের প্রথম লাইন। এটাই নিরাপত্তার প্রথম প্রতিরক্ষা |
| `actions/orders.ts` | `placeOrder()` (রেট লিমিট চেক + `create_order()` RPC + কুকি সেট), `trackOrder()`, `checkCoupon()` |
| `actions/admin.ts` | অ্যাডমিনের সব লেখা — বই যোগ/সম্পাদনা (`readBookForm`, `readGallery` সহ), স্টক, স্টেটাস, পেমেন্ট, রিভিউ, সেটিংস |
| `actions/book-import.ts` | `bulkImportBooks()` — সার্ভারে আবার যাচাই করে ব্যাচে ইনসার্ট |
| `actions/courier.ts` | কুরিয়ারে বুকিং ও স্টেটাস সিঙ্ক |
| `actions/wishlist.ts` | আইডি তালিকা দিয়ে বই আনা |

### কুরিয়ার মডিউল

| ফাইল | ভূমিকা |
|---|---|
| `courier/types.ts` | কুরিয়ার ইন্টারফেস ও স্টেটাস ম্যাপিং। ✅ টেস্ট করা |
| `courier/steadfast.ts` | SteadFast ইন্টিগ্রেশন — base URL `portal.packzy.com/api/v1`, হেডার `Api-Key`/`Secret-Key` |
| `courier/index.ts` | প্রোভাইডারের তালিকা — **নতুন কুরিয়ার যোগ করতে শুধু এখানে নাম বসাতে হয়** |

---

## ২.৭ `src/types/`, `tests/`, `scripts/`, `.github/`

| ফাইল | ধরন | ভূমিকা |
|---|---|---|
| `src/types/database.ts` | TS | **ডেটাবেসের সব সারির টাইপ** — হাতে লেখা, যাতে প্রজেক্ট ক্লোন করে `npm install` করলেই টাইপ পাওয়া যায়। স্কিমা বদলালে এটাও বদলাতে হবে (বা `npm run db:types`) |

### `tests/` — ৯২টি ইউনিট টেস্ট, ৬টি ফাইল

| ফাইল | কী পরীক্ষা করে |
|---|---|
| `format.test.ts` | টাকার ফরম্যাট, বাংলা সংখ্যা, ছাড়ের শতাংশ, স্টকের অবস্থা |
| `validation.test.ts` | বাংলাদেশি ফোন (স্পেস/ড্যাশ/+৮৮০), চেকআউটের প্রতিটি নিয়ম |
| `csv.test.ts` | CSV পার্সিং — কোটেশন, এস্কেপ, নতুন লাইন, BOM, সেমিকোলন |
| `book-import.test.ts` | ইমপোর্ট সারির ভ্যালিডেশন, টেমপ্লেট |
| `constants.test.ts` | ৮ বিভাগ ও মোট ৬৪ জেলা, লেবেলের সম্পূর্ণতা |
| `courier.test.ts` | কুরিয়ার স্টেটাস → অর্ডার স্টেটাস ম্যাপিং |

### অন্যান্য

| ফাইল | ধরন | ভূমিকা |
|---|---|---|
| `scripts/bundle-migrations.mjs` | JS | ৭টি migration জোড়া দিয়ে `RUN_ALL_IN_SQL_EDITOR.sql` বানায় (`npm run db:bundle`) |
| `.github/workflows/ci.yml` | YAML | GitHub Actions — প্রতি push-এ টাইপ চেক → টেস্ট → বিল্ড |

---

## ২.৮ কোন ফাইল ছোঁয়া যাবে না

| ফাইল | কারণ |
|---|---|
| `next-env.d.ts`, `tsconfig.tsbuildinfo` | স্বয়ংক্রিয়ভাবে তৈরি হয় |
| `supabase/RUN_ALL_IN_SQL_EDITOR.sql` | জেনারেট করা — হাতে এডিট করলে পরেরবার মুছে যাবে |
| `supabase/migrations/` এর পুরনো ফাইল | ডেটাবেস ও কোডের ফারাক তৈরি হবে; নতুন ফাইল যোগ করুন |
| `package-lock.json` | npm নিজে সামলায় |
| `.env.local` | গোপন চাবি — নতুন করে বানাতে হয়, এডিট করে ভাগ করা যায় না |

---

# ভাগ ৩ — চলমান তথ্য

## ৩.১ সংযোগ ও ঠিকানা

| জিনিস | মান |
|---|---|
| **লাইভ সাইট** | https://boighor-ochre.vercel.app |
| **অ্যাডমিন লগইন** | https://boighor-ochre.vercel.app/login |
| **অ্যাডমিন প্যানেল** | https://boighor-ochre.vercel.app/admin |
| **অ্যাডমিন ইমেইল** | `arefatulrumon77@gmail.com` |
| **GitHub রিপো** | https://github.com/arefatulrumon/boighor- |
| **Git শাখা** | `main` |
| **Supabase প্রজেক্ট** | `vnmmvhalpqwwihxbldmy` (রিজিয়ন: Singapore) |
| **Vercel প্রজেক্ট** | `boighor` — id `prj_Jk5qckpaDMUwKfEekXCSaczFezNo`, টিম `rumon1` |
| **প্রযোজনা রিজিয়ন** | `sin1` (Singapore) — বাংলাদেশের জন্য সবচেয়ে কাছের |
| **Cron** | `/api/cron/courier-sync` · `0 3 * * *` (সকাল ৯টা বাংলাদেশ সময়) |

**বর্তমান অবস্থা:** সাইট লাইভ, ডেটাবেসে ৭টি migration প্রয়োগ করা, ৫টি ডেমো বই আছে, রিভিউ ও সেটিংস তৈরি। বিল্ড ও রানটাইম — দুটোই যাচাই করা।

## ৩.২ গোপন চাবি কোথায় থাকে

| জায়গা | কী আছে |
|---|---|
| `bookstore/.env.local` | সব চাবি (স্থানীয় ডেভেলপমেন্টের জন্য) — **`.gitignore`-এ আছে, Git-এ যায় না** |
| Vercel → Settings → Environment Variables | একই চাবি (লাইভ সাইটের জন্য), `sensitive` ধরনের |
| Supabase → Settings → API Keys | চাবির মূল উৎস |

**⚠️ নিরাপত্তার নিয়ম — এই ডকুমেন্টে কোনো চাবি লেখা নেই, এবং লেখা উচিতও নয়:**

- এই ডকুমেন্টটি ভাগাভাগি করার জন্য তৈরি — তাই এখানে **`SUPABASE_SECRET_KEY` ইচ্ছাকৃতভাবে বাদ দেওয়া হয়েছে**। ওই চাবি RLS সম্পূর্ণ বাইপাস করে, অর্থাৎ যার হাতে আছে সে পুরো ডেটাবেস পড়তে ও লিখতে পারে।
- যে কেউ প্রকল্পের ফোল্ডারে ঢুকতে পারে, সে `.env.local` খুলে চাবিগুলো পাবে — তাই আলাদা করে কোথাও কপি করার দরকার নেই।
- **`NEXT_PUBLIC_` দিয়ে শুরু হওয়া চাবি গোপন নয়** — সেগুলো ব্রাউজারে যায়, নিরাপত্তা আসে RLS থেকে।
- চাবি কখনো চ্যাট, ইমেইল বা স্ক্রিনশটে পাঠাবেন না। যদি কখনো পাঠাতে হয়, কাজ শেষে অবশ্যই **rotate** করুন (নতুন চাবি বানিয়ে পুরনোটা মুছে ফেলুন)।

## ৩.৩ কমান্ড

```bash
npm run dev          # ডেভেলপমেন্ট সার্ভার → http://localhost:3000
npm run build        # প্রোডাকশন বিল্ড
npm start            # বিল্ড করা অ্যাপ চালানো
npm run test         # ৯২টি ইউনিট টেস্ট (~২ সেকেন্ডে)
npm run typecheck    # শুধু টাইপ চেক
npm run check        # typecheck + test + build — কমিট করার আগে এটাই চালান
npm run db:bundle    # ৭টি migration জোড়া দিয়ে একটা ফাইল বানানো
npm run db:types     # ডেটাবেস থেকে টাইপ অটো-জেনারেট (supabase CLI লাগে)
```

**পরিবর্তন করার পরের ধারা:**

```bash
npm run check                                  # সব ঠিক আছে কি?
git add -A
git commit -m "কী বদলালেন"
git push origin main                           # Vercel নিজেই লাইভ করে দেবে
```

## ৩.৪ রুটিন কাজ

### নতুন বই যোগ করা

- **অনেক বই:** `/admin/books/import` → টেমপ্লেট নামান → পূরণ করুন → আপলোড করে প্রিভিউ দেখে ইমপোর্ট
- **একটা বই:** `/admin/books/new` → ছবি টেনে ছাড়ুন (নিজেই ছোট হয়ে WebP-তে যাবে)

### দৈনিক অর্ডার প্রক্রিয়া

1. `/admin/orders` → **নতুন** ট্যাব
2. কাস্টমারকে ফোন করে নিশ্চিত করুন → স্টেটাস **নিশ্চিত হয়েছে**
3. bKash পেমেন্ট হলে TrxID মিলিয়ে **পরিশোধিত** চিহ্নিত করুন
4. বই পাঠান — কুরিয়ার সংযুক্ত থাকলে "কুরিয়ারে বুক করুন"; না থাকলে "কুরিয়ারের জন্য CSV" নামিয়ে পোর্টালে পেস্ট করুন
5. বাকিটা Cron নিজেই দেখে নেবে — ডেলিভারি হলে স্টেটাস ও পেমেন্ট আপডেট হবে

### ব্যাকআপ (গুরুত্বপূর্ণ!)

Supabase-এর **Free প্ল্যানে স্বয়ংক্রিয় ব্যাকআপ নেই**। মাসে অন্তত একবার:

```sql
-- Supabase SQL Editor-এ চালিয়ে ফাইল হিসেবে নামিয়ে রাখুন
select * from public.orders order by created_at desc;
select * from public.order_items;
select * from public.books;
```

অথবা Dashboard → Database → Backups (Pro প্ল্যানে)।

## ৩.৫ সমস্যা হলে — দ্রুত খোঁজ

| লক্ষণ | কারণ ও সমাধান |
|---|---|
| **সাইট লোড হচ্ছে কিন্তু বই নেই** | Supabase প্রজেক্ট pause হয়ে গেছে (৭ দিন নিষ্ক্রিয় থাকলে হয়) → Dashboard → Restore |
| **বিল্ড ফেল করল: "পরিবেশ ভেরিয়েবল ... পাওয়া যায়নি"** | Vercel-এ ওই env ভেরিয়েবলটি নেই বা মান খালি → Settings → Environment Variables |
| **Vercel ডিপ্লয় ফেল: "Hobby accounts are limited to daily cron jobs"** | `vercel.json`-এ Cron দিনে একবারের বেশি → `0 3 * * *` রাখুন |
| **`/admin`-এ গেলে `/login`-এ ফিরে আসে** | ইউজারটি `public.staff`-এ নেই বা `is_active = false` |
| **e-mail লিংক localhost-এ যাচ্ছে** | Supabase-এর Site URL এখনো `http://localhost:3000` → §১.৫ দেখুন |
| **সেক্রেট চাবি দিয়ে API কলে ৪০১** | কী ভুল নয়! Supabase ব্রাউজার `User-Agent` দেখে সেক্রেট চাবি ব্লক করে → `curl`/`node` ধরনের UA দিন |
| **অর্ডার করতে গিয়ে "RATE_LIMITED"** | রেট লিমিট কাজ করছে (১০ মিনিটে ৩টি) — এটি বাগ নয় |
| **"পাসওয়ার্ড ঠিক নেই" কিন্তু ভুলও লেখেননি** | অ্যাকাউন্টে হয়তো পাসওয়ার্ড সেটই হয়নি → "পাসওয়ার্ড ভুলে গেছেন?" থেকে নতুন সেট করুন |

### নিরাপত্তার পরীক্ষা — নিজে যাচাই করুন

```sql
-- ১) সব টেবিলে RLS চালু আছে কি?
select tablename, rowsecurity from pg_tables where schemaname = 'public';

-- ২) কে staff?
select s.full_name, s.role, s.is_active, u.email
from public.staff s join auth.users u on u.id = s.user_id;
```

```bash
# ৩) সাধারণ ব্যবহারকারী অর্ডার পড়তে পারে কি না — ৪০১ আসা উচিত
curl -s -o /dev/null -w "%{http_code}\n" \
  "https://vnmmvhalpqwwihxbldmy.supabase.co/rest/v1/orders?select=id" \
  -H "apikey: sb_publishable_bYih3KSOcE_r-uaLTxHYGQ_fLIvO2g5"
# প্রত্যাশিত: 401  ← এটাই ঠিক আছে
```

```bash
# ৪) Cron এন্ডপয়েন্ট সুরক্ষিত কি না — ৪০১ আসা উচিত
curl -s -o /dev/null -w "%{http_code}\n" \
  https://boighor-ochre.vercel.app/api/cron/courier-sync
# প্রত্যাশিত: 401
```

## ৩.৬ নিরাপত্তার তিন স্তর — যা কখনো ভাঙবেন না

```
স্তর ১: proxy.ts              → "লগইন করা আছে?" (সেশন রিফ্রেশ)
স্তর ২: admin/layout.tsx      → "staff কি?" — না হলে /login
স্তর ৩: PostgreSQL RLS        → আসল রক্ষাকবচ, ডেটাবেস স্তরে
        + requireStaff()      → প্রতিটি Server Action-এ
```

**সবচেয়ে গুরুত্বপূর্ণ:** `orders` টেবিলে সাধারণ ব্যবহারকারীর (anon) **কোনো সরাসরি অ্যাক্সেস নেই**। অর্ডার তৈরি হয় শুধু `create_order()` ফাংশন দিয়ে, যেখানে দাম ডেটাবেস থেকে পড়া হয়, স্টক `FOR UPDATE` দিয়ে লক করা হয়, কুপন সার্ভারে যাচাই হয়।

**ফলে:** কেউ ব্রাউজারের DevTools খুলে দাম ৳৪৫০ থেকে ৳১ করে দিতে পারবে না।

নতুন কোনো ফিচার যোগ করার সময় এই তিন স্তর ভাঙবেন না — বিস্তারিত [`AGENTS.md`](AGENTS.md) §"যা কখনো করবেন না"।

---

## সারসংক্ষেপ — এক নজরে

**কী:** বাংলা অনলাইন বইয়ের দোকান, একক বিক্রেতার জন্য
**কী দিয়ে:** Next.js 16 + TypeScript + Tailwind v4 + Supabase + Vercel
**অবস্থা:** লাইভ — https://boighor-ochre.vercel.app
**ফাইল:** ১০৪টি · **টেস্ট:** ৯২টি · **ডেটাবেস migration:** ৭টি
**পরের কাজ:** Supabase Site URL ঠিক করা (§১.৫) → তারপর আসল বই যোগ করা

**কেউ নতুন এসে কাজ শুরু করতে চাইলে এই ক্রমে পড়ুন:**

1. এই ফাইল (`HANDOVER.md`) — এখন যেখানে আছেন
2. [`README.md`](README.md) — ফিচার ও কমান্ড
3. [`ARCHITECTURE.md`](ARCHITECTURE.md) — সিস্টেম কীভাবে কাজ করে
4. [`AGENTS.md`](AGENTS.md) — কাজ করার নিয়ম ও রেসিপি
5. [`DECISIONS.md`](DECISIONS.md) — কেন এমনভাবে বানানো

_[HANDOVER.md শেষ]_
