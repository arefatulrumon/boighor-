# রোডম্যাপ — পরবর্তী কাজ

> **নীতি:** একটা একটা করে শেষ করবেন, একসাথে অনেক কিছু শুরু করবেন না।
> প্রতিটি আইটেম নিজে থেকেই সম্পূর্ণ ব্যবহারযোগ্য — মাঝপথে ফেলে রাখার দরকার নেই।

---

## ধাপ ১ — চালু করা (এখনই)

লক্ষ্য: সাইট লাইভ, প্রথম অর্ডার গ্রহণ।

- [ ] Supabase প্রজেক্ট তৈরি (Region: Singapore)
- [ ] ৫টি migration চালানো
- [ ] `public.staff` এ নিজের অ্যাকাউন্ট যোগ করা
- [ ] `.env.local` এ আসল কী বসানো
- [ ] GitHub এ পুশ + Vercel এ ডিপ্লয় + env ভেরিয়েবল সেট
- [ ] ডেমো বই মুছে আসল বই যোগ করা (`delete from public.books where slug like 'demo-%'`)
- [ ] ডেলিভারি চার্জ আসল কুরিয়ার রেট অনুযায়ী বদলানো
- [ ] `site_settings` এ আসল ফোন, ইমেইল, bKash/Nagad নম্বর বসানো
- [ ] ডেমো কুপন বন্ধ করা (`update public.coupons set is_active = false where code in ('WELCOME10','BOIMELA50')`)
- [ ] একবার নিজে পুরো একটা টেস্ট অর্ডার করা (সবচেয়ে গুরুত্বপূর্ণ ধাপ)

---

## ধাপ ২ — ব্যবসার জন্য জরুরি

### ✅ ২.১ ছবি আপলোডার — শেষ হয়েছে
`src/components/admin/image-uploader.tsx`

- ড্র্যাগ-অ্যান্ড-ড্রপ (কভার + গ্যালারি, সর্বোচ্চ ১২টি ছবি)
- **আপলোডের আগেই ছবি ছোট হয়ে যায়** — সর্বোচ্চ ১৪০০px, WebP (প্রায় ৭০% কম)
- ব্রাউজার থেকে সরাসরি Supabase Storage এ যায় → কোনো সার্ভার সিক্রেট লাগে না
- ফোল্ডার: `book-covers/<book-id>/`
- ছবি না দিলে টাইটেল দিয়ে সুন্দর প্লেসহোল্ডার

### ✅ ২.২ CSV ইমপোর্ট — শেষ হয়েছে
`/admin/books/import` · `src/lib/book-import.ts` · `src/components/admin/csv-import.tsx`

- একবারে ২০০০ বই, আপলোডের সাথে সাথেই প্রতি সারির যাচাই
- একই ভ্যালিডেশন কোড ক্লায়েন্ট ও সার্ভার দুই জায়গায় (অমিল হওয়ার সুযোগ নেই)
- বাংলা সংখ্যা (৪৫০), বাংলা ভাষার নাম (বাংলা/ইংরেজি), `হ্যাঁ/না` — সব বুঝে নেয়
- Excel ভাঙা এড়াতে BOM সহ নমুনা ফাইল ডাউনলোড

### ✅ ২.৩ কুরিয়ার ইন্টিগ্রেশন (SteadFast) — শেষ হয়েছে
`src/lib/courier/` · `src/lib/actions/courier.ts` · `/api/cron/courier-sync`

- অ্যাডমিন প্যানেল থেকে এক ক্লিকে কুরিয়ারে বুকিং
- প্রতিদিন একবার Cron স্টেটাস সিঙ্ক → ডেলিভারি হলে অর্ডার নিজে থেকেই আপডেট, COD এ পেমেন্টও "পরিশোধিত"
- API কী না থাকলেও কাজ চলে: ট্র্যাকিং কোড হাতে লিখুন, অথবা **"কুরিয়ারের জন্য CSV"** এক্সপোর্ট করে পোর্টালে পেস্ট করুন
- নতুন কুরিয়ার (Pathao/RedX) যোগ করতে `CourierProvider` ইন্টারফেস মানুন — বাকি কোড ছুঁতে হবে না

### ✅ ২.৪ রেট লিমিট — শেষ হয়েছে
`supabase/migrations/20260924000006_rate_limit.sql`

- ১০ মিনিটে ৩টি, ২৪ ঘণ্টায় ১০টি (একই ফোন নম্বর থেকে)
- দুই স্তর: অ্যাপ আগে চেক করে (সুন্দর বার্তা), ডেটাবেসে ট্রিগার পাহারা দেয় (রেস-কন্ডিশন বন্ধ, `pg_advisory_xact_lock` দিয়ে)
- অ্যাডমিনের তৈরি অর্ডার লিমিটে পড়ে না

### ২.৫ অর্ডার কনফার্মেশন SMS — বাকি
**কেন:** কাস্টমার ট্র্যাক কোড পেলে ফোন কম আসে।
**কী:** Supabase Database Webhook (orders INSERT) → Edge Function →
SMS (SSLWireless / Alpha SMS)।
**যা লাগবে:** SMS প্রোভাইডার অ্যাকাউন্ট ও API কী।

---

## ধাপ ৩ — ব্যবসা বাড়ানো

### ✅ ৩.১ উইশলিস্ট — শেষ হয়েছে
`/wishlist` · `src/lib/wishlist.tsx`

লগইন ছাড়াই "পরে কিনব" তালিকা (localStorage)। ডেটাবেস লাগে না, ক্রেতার
অ্যাকাউন্টও লাগে না — গেস্ট চেকআউটের সাথে পুরোপুরি মানানসই।

### ✅ ৩.২ অ্যানালিটিক্স — শেষ হয়েছে
Vercel Analytics + Speed Insights (`src/app/layout.tsx`)।
ডিপ্লয়ের পর Vercel ড্যাশবোর্ডে কে কোথা থেকে আসছে, কোন পেজ ধীর —
সব দেখা যাবে। কোনো কুকি বা অতিরিক্ত খরচ নেই।

### ৩.৩ কাস্টমার অ্যাকাউন্ট (ঐচ্ছিক, আগে দরকার নেই)
`orders.user_id` কলাম যোগ → পুরনো অর্ডার null থাকবে।
**সতর্কতা:** গেস্ট চেকআউট **রাখতে হবে** — অ্যাকাউন্ট বাধ্যতামূলক করলে
অর্ডার কমবে। এটা বাড়তি সুবিধা, শর্ত নয়।

### ৩.৪ উন্নত সার্চ
`.ilike` এর বদলে PostgreSQL `pg_trgm` `similarity()` সর্টিং, অথবা
Meilisearch। এখনকার সার্চ ছোট ক্যাটালগে (<১০০০ বই) যথেষ্ট ভালো।

### ৩.৫ স্টক সতর্কতা ইমেইলে
প্রতিদিন সকালে Cron → কম স্টকের বইয়ের তালিকা ইমেইলে।
(`/api/cron/courier-sync` ফাইলটাই মডেল হিসেবে ব্যবহার করুন)

---

## ধাপ ৪ — নিরাপত্তা ও নির্ভরযোগ্যতা

- [x] **রেট লিমিট** — ফোন-ভিত্তিক, দুই স্তরে (মাইগ্রেশন ০০০৬)
- [x] **বিশুদ্ধ ফাংশনের টেস্ট** — ৯২টি টেস্ট (`npm run test`)
- [x] **CI** — GitHub Actions এ typecheck → test → build
- [ ] **ডেটাবেসের টেস্ট** — `create_order()` এর ব্যবসার নিয়ম (স্টক শেষ,
      ভুল কুপন, একই সাথে দুই অর্ডার)। আসল Supabase প্রজেক্ট লাগবে,
      তাই আলাদা টেস্ট প্রজেক্ট বানানো ভালো।
- [ ] **IP-ভিত্তিক রেট লিমিট** — `orders.ip_hash` কলাম আছে কিন্তু ব্যবহার
      হয় না। VPN দিয়ে ফোন নম্বর বদলে স্প্যাম করা এখনো সম্ভব।
- [ ] **ব্যাকআপ** — মাসিক CSV এক্সপোর্টের রুটিন
- [ ] **Cron keep-alive** — Free plan এর ৭ দিনের পজ এড়াতে দৈনিক হালকা পিং

---

## ধাপ ৫ — সম্প্রসারণ (শুধু চাহিদা থাকলে)

| ফিচার | কতটা কাজ |
|---|---|
| অটোমেটেড পেমেন্ট (SSLCommerz / bKash Merchant) | বড় — ব্যবসার নিবন্ধন লাগবে |
| ইংরেজি ভার্সন (i18n) | মাঝারি — সব লেবেল দুই ভাষায় |
| প্রি-অর্ডার | মাঝারি — `books.is_preorder` + আগমন তারিখ |
| প্যাকেজ/বান্ডেল | মাঝারি — নতুন `bundles` টেবিল |
| e-বুক বিক্রি | মাঝারি — ফাইল ডেলিভারি + ডাউনলোড লিংক |
| মাল্টি-ভেন্ডর | বড় — দেখুন DECISIONS.md → ADR-001 |

---

## ইচ্ছাকৃতভাবে যা করা হবে না

| কী | কেন |
|---|---|
| কাস্টমার অ্যাকাউন্ট বাধ্যতামূলক | গেস্ট চেকআউটেই বেশি অর্ডার আসে |
| অ্যাপ-সাইড দাম হিসাব | সরাসরি আর্থিক ঝুঁকি (ADR-003) |
| জটিল state management লাইব্রেরি | এই আকারে React Context যথেষ্ট |
| ডেটাবেসে কার্ট রাখা | অপ্রয়োজনীয় জটিলতা (ADR-002) |
| বাংলা সংখ্যা ডেটাবেসে | সব `SUM`/`ORDER BY` ভেঙে যাবে (ADR-009) |

---

## অগ্রাধিকার নির্ধারণের নিয়ম

নতুন কিছু যোগ করার আগে তিনটি প্রশ্ন:

1. **এটা কি দিনে ১০ মিনিট বাঁচায় বা বিক্রি বাড়ায়?** না হলে পরে।
2. **এটা কি ভাঙা কিছু সারায়?** হ্যাঁ হলে সবার আগে।
3. **এটা কি বিদ্যমান কোনো সিদ্ধান্তের সাথে সাংঘর্ষিক?** `DECISIONS.md` দেখুন।

---

## অগ্রগতি লিপিবদ্ধ করুন

প্রতিটি উল্লেখযোগ্য কাজ শেষ হলে `DECISIONS.md` এ কারণ লিখুন (বিশেষ করে
যদি আগের সিদ্ধান্ত বদলান)। এতে দুই মাস পরে নিজেই বুঝতে পারবেন কেন
এমন করা হয়েছিল।
